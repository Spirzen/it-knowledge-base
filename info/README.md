# Служебные файлы репозитория

Содержимое этой папки **не входит** в сборку Docusaurus (только `docs/`).

| Файл | Описание |
|------|----------|
| `demo-registry.md` | Реестр React-демо и статей (`npm run docs:demo-registry`) |
