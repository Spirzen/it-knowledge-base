---
tags: [all, beginner, developer]
title: "Scikit-learn — регрессия и классификация"
description: "Табличный ML на Python — Pipeline, метрики, LinearRegression, LogisticRegression, RandomForest и GridSearchCV без утечки данных."
related:
  - title: "Сквозной проект — Мельбурн"
    doc: encyclopedia/6-ai/6-02-mashinnoe-obuchenie/7
  - title: "Разбиение данных и кросс-валидация"
    doc: encyclopedia/6-ai/6-02-mashinnoe-obuchenie/6
  - title: "Кодирование категориальных признаков"
    doc: encyclopedia/6-ai/6-02-mashinnoe-obuchenie/4
  - title: "Машинное обучение"
    doc: encyclopedia/6-ai/6-02-mashinnoe-obuchenie/1
  - title: "Keras и TensorFlow — нейросети"
    doc: encyclopedia/6-ai/6-03-neyroseti/114
  - title: "Python для анализа данных"
    doc: encyclopedia/3-data-markup/3-11-analiz-dannyh/424
  - title: "Titanic — LR, дерево и Keras"
    doc: lab/examples/1157
  - title: "Spam — TF-IDF"
    doc: lab/examples/1158
  - title: "Insurance — регрессия charges"
    doc: lab/examples/1159
---

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


# Scikit-learn — регрессия и классификация

<div class="article-tags">
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>

**Scikit-learn** — стандартная библиотека для **табличного** машинного обучения на Python — регрессия, классификация, кластеризация, предобработка и оценка моделей в едином API. Данные в sklearn — массивы NumPy; базовые операции — [NumPy — массивы и матрицы](/lab/Примеры/1129). Для изображений, длинных текстов и больших нейросетей обычно переходят к TensorFlow или PyTorch — см. [Keras и TensorFlow](/encyclopedia/6-ai/6-03-neyroseti/114). Обзор всех типов обучения — в [Машинное обучение](/encyclopedia/6-ai/6-02-mashinnoe-obuchenie/1); практический end-to-end пример — [проект Melbourne](/encyclopedia/6-ai/6-02-mashinnoe-obuchenie/7).

---

## Когда достаточно scikit-learn

| Задача | Типичные алгоритмы sklearn |
|--------|----------------------------|
| Прогноз числа (цена, спрос) | `LinearRegression`, `GradientBoostingRegressor`, `RandomForestRegressor` |
| Категория (спам, отток) | `LogisticRegression`, `RandomForestClassifier`, `SVC` |
| Группы без меток | `KMeans`, `DBSCAN` |
| Сжатие признаков | `PCA`, `TruncatedSVD` |

Данные — таблица: строки — объекты, столбцы — признаки. Перед обучением нужны [кодирование категорий](/encyclopedia/6-ai/6-02-mashinnoe-obuchenie/4) и [честное разбиение train/test](/encyclopedia/6-ai/6-02-mashinnoe-obuchenie/6).

---

## Единый контракт API

Почти все модели sklearn поддерживают три метода:

- `fit(X, y)` — обучение;
- `predict(X)` — прогноз;
- `score(X, y)` — быстрая оценка (для классификаторов — accuracy по умолчанию).

```python
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
from sklearn.ensemble import RandomForestClassifier

X, y = load_iris(return_X_y=True)
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.25, random_state=42, stratify=y
)

clf = RandomForestClassifier(n_estimators=100, random_state=42)
clf.fit(X_train, y_train)
print(clf.score(X_test, y_test))
```

---

## Регрессия — прогноз числа

**Регрессия** предсказывает непрерывную величину `y` (цена, температура, время доставки).



<ExternalCodeEmbed example="python/ai-6-6-02-mashinnoe-obuchenie-10-001" title="Регрессия — прогноз числа" minHeight={318} />



| Метрика | Смысл |
|---------|--------|
| **MAE** | Средняя абсолютная ошибка в единицах целевой переменной |
| **RMSE** | Штрафует крупные промахи сильнее MAE |
| **R²** | Доля дисперсии, объяснённая моделью (1.0 — идеально на train, на test обычно ниже) |

Сквозной пример с **GradientBoostingRegressor** и **GridSearchCV** — [Мельбурн](/encyclopedia/6-ai/6-02-mashinnoe-obuchenie/7). Пошаговая линейная регрессия с `coef_` — [Lab 1159 — Insurance](/lab/Примеры/1159).

### Интерпретация `coef_` и `intercept_`

После `LinearRegression().fit(X, y)`:

- **`intercept_`** — прогноз при нулевых признаках (смещение прямой);
- **`coef_[i]`** — на сколько в среднем меняется `y`, если признак `i` увеличить на 1 (остальные фиксированы).

```python
import matplotlib.pyplot as plt
import numpy as np
from sklearn.linear_model import LinearRegression

# один признак для наглядности
X1 = np.array([[1], [2], [3], [4], [5]], dtype=float)
y1 = np.array([2.1, 4.0, 5.9, 8.2, 10.0])

reg = LinearRegression()
reg.fit(X1, y1)
print("intercept:", reg.intercept_)
print("coef:", reg.coef_)

x_line = np.linspace(0, 6, 50)
plt.scatter(X1, y1, label="данные")
plt.plot(x_line, reg.predict(x_line.reshape(-1, 1)), "r", label="модель")
plt.legend(); plt.grid(alpha=0.3); plt.show()
```

На нескольких признаках уравнение: `y = b + w₁x₁ + w₂x₂ + …`. Знак `w` — направление связи; величина — сила **при линейной** модели.

### МНК vs градиентный спуск (`SGDRegressor`)

| | `LinearRegression` | `SGDRegressor` |
|---|-------------------|----------------|
| Метод | Закрытая формула (МНК) | Итеративный градиентный спуск |
| Масштаб данных | Устойчив на малых таблицах | Чувствителен — нужен `StandardScaler` |
| Большие данные | Медленнее на миллионах строк | Потоковое `partial_fit` |
| Когда брать | Дефолт для учебных таблиц | Онлайн-обучение, очень большие матрицы |

```python
from sklearn.linear_model import SGDRegressor
from sklearn.preprocessing import StandardScaler
from sklearn.pipeline import Pipeline

sgd_pipe = Pipeline([
    ("scaler", StandardScaler()),
    ("reg", SGDRegressor(max_iter=5000, tol=1e-3, random_state=42)),
])
sgd_pipe.fit(X_train, y_train)
```

Идея градиентного спуска — в [перцептроне на NumPy](/encyclopedia/6-ai/6-03-neyroseti/2#градиентный-спуск-руками); Keras делает то же автоматически.

---

## Классификация — прогноз категории

**Классификация** предсказывает метку класса — 0/1, "кошка"/"собака", один из нескольких видов.

```python
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import classification_report, confusion_matrix

clf = LogisticRegression(max_iter=500)
clf.fit(X_train, y_train)
y_pred = clf.predict(X_test)

print(classification_report(y_test, y_pred))
print(confusion_matrix(y_test, y_pred))
```

При **дисбалансе классов** (99% "не спам", 1% "спам") accuracy вводит в заблуждение. Смотрите **precision**, **recall**, **F1** и матрицу ошибок — подробнее в разделе метрик [Машинное обучение](/encyclopedia/6-ai/6-02-mashinnoe-obuchenie/1).

### Как читать confusion matrix

Для **бинарной** классификации (классы 0 и 1) матрица 2×2:

| | Предсказано 0 | Предсказано 1 |
|---|---------------|---------------|
| **Истина 0** | TN (верно «нет») | FP (ложная тревога) |
| **Истина 1** | FN (пропуск) | TP (верно «да») |

```python
from sklearn.metrics import ConfusionMatrixDisplay

cm = confusion_matrix(y_test, y_pred)
ConfusionMatrixDisplay(cm, display_labels=["class_0", "class_1"]).plot()
```

- **Accuracy** = (TP + TN) / всего — доля всех верных ответов.
- **Recall** для класса 1 = TP / (TP + FN) — сколько реальных «единиц» нашли.
- **Precision** для класса 1 = TP / (TP + FP) — сколько из предсказанных «единиц» верны.

Сквозной пример с тремя моделями — [Lab 1157 — Titanic](/lab/Примеры/1157); spam и TF-IDF — [Lab 1158](/lab/Примеры/1158).

`predict_proba(X)` возвращает вероятности по классам — удобно для порога "срабатывания" в проде.

У **логистической регрессии** знак `coef_[i]` показывает, как признак влияет на лог-шанс класса 1; модуль — сила при прочих равных. На [Titanic](/lab/Примеры/1157) удобно сравнить, какой признак сильнее связан с выживанием.

---

## Pipeline — без утечки данных

Ошибка новичка: сначала `StandardScaler` на **всей** таблице, потом `train_test_split`. Статистики scaler "видят" test — метрики завышаются.

**Pipeline** объединяет предобработку и модель; `fit` на train применяет scaler только к train.

```python
from sklearn.pipeline import Pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.svm import SVC

pipe = Pipeline([
    ("scaler", StandardScaler()),
    ("clf", SVC(kernel="rbf", probability=True)),
])
pipe.fit(X_train, y_train)
print(pipe.score(X_test, y_test))
```

Тот же pipeline можно передать в **GridSearchCV** — перебор гиперпараметров внутри кросс-валидации на train.

---

## Подбор гиперпараметров



<ExternalCodeEmbed example="python/ai-6-6-02-mashinnoe-obuchenie-10-002" title="Подбор гиперпараметров" minHeight={372} />



`cv=5` — пять фолдов на **train**; **test** остаётся для финального отчёта один раз.

---

## Сохранение модели

```python

import joblib

joblib.dump(pipe, "model.joblib")
loaded = joblib.load("model.joblib")
loaded.predict(X_test[:1])
```

В проде версионируйте и **модель**, и **схему признаков** (имена и типы столбцов), иначе после переобучения API сломается.

---

## Связь с другими материалами

| Тема | Статья |
|------|--------|
| Алгоритмы (деревья, SVM, бустинг) | [Алгоритмы ИИ](/encyclopedia/6-ai/6-02-mashinnoe-obuchenie/2) |
| Titanic — LR, дерево, Keras | [Lab 1157](/lab/Примеры/1157) |
| Insurance — регрессия charges | [Lab 1159](/lab/Примеры/1159) |
| Diabetes — logistic + NN | [Lab 1160](/lab/Примеры/1160) |
| Spam — TF-IDF | [Lab 1158](/lab/Примеры/1158) |
| Pandas и EDA | [Python для анализа](/encyclopedia/3-data-markup/3-11-analiz-dannyh/424) |
| Нейросети, изображения, текст | [Keras и TensorFlow](/encyclopedia/6-ai/6-03-neyroseti/114), [распознавание](/encyclopedia/6-ai/6-06-primenenie-ii/120) |
| Облачные API без своего обучения | [Cognitive Services](/encyclopedia/6-ai/6-05-razrabotka-ii/120) |
