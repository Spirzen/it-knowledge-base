---
tags: [engineer, developer, architector, required, beginner]
title: Команды Git для повседневной разработки
description: git fetch используется для просмотра данных в репозитории.
---

import GitCommandsPlay from '@site/src/components/GitCommandsPlay.jsx';

# Команды Git для повседневной разработки

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>
<span class="complexity-badge">Инженеру</span>

:::tip Связь с разделом 4

Первый проход по Git — в [Основы работы с Git](/encyclopedia/4-code-dev/4-13-osnovy-raboty-s-git/intro): [workflow add/commit/push](/encyclopedia/4-code-dev/4-13-osnovy-raboty-s-git/112), [ветки и запросы на слияние](/encyclopedia/4-code-dev/4-13-osnovy-raboty-s-git/113). Эта статья дополняет его **справочником команд** и короткими пояснениями.

:::

---

## Команды Git для повседневной разработки

<GitCommandsPlay />

---

### Основные команды

#### git init

Инициализация нового локального репозитория в текущей каталоге (создаётся скрытая папка `.git`):

```bash
git init
git init my-project
```

---

#### git status

Проверка состояния рабочего каталога и индекса:

```bash
git status
git status -sb
```

---

#### git add

Добавление изменений из рабочего каталога в индекс (первый шаг перед коммитом):

```bash
git add README.md # Добавить конкретный файл
git add .         # Добавить все изменённые файлы
```

---

#### git commit

Фиксация изменений из индекса с сообщением через `-m`:

```bash
git commit -m "Добавил файл – это сообщение-комментарий."
```

---

#### git diff

Просмотр различий между рабочим каталогом, индексом и последним коммитом:

```bash
git diff
git diff --staged
git diff main..feature
```

---

#### git log

История коммитов с настраиваемым форматом:

```bash
git log --oneline --graph --decorate
git log -5 --stat
```

---

#### git push

Отправка локальных коммитов на удалённый репозиторий:

```bash
git push origin main
```

---

#### git pull

Получение изменений с remote и слияние с текущей веткой:

```bash
git pull origin main
```

---

#### git branch

Создание, просмотр и удаление веток:

```bash
git branch
git branch feature/login
git branch -d feature/login
```

---

#### git checkout

Переключение ветки или восстановление файла из коммита:

```bash
git switch feature-branch
git checkout abc1234 -- path/to/file
```

---

#### git merge

`git merge` объединяет изменения из одной ветки в другую:

```bash
git merge feature-branch
```

---

#### git reset

`git reset` отменяет изменения, перемещая HEAD. Мягкий и жёсткий варианты:

```bash
git reset --soft HEAD~1
git reset --hard HEAD~1
```

---

#### git rm

Удаление файла из индекса и рабочего каталога:

```bash
git rm path/to/file
git rm --cached path/to/file
```

---

#### git mv

`git mv` – переименование или перемещение файлов в рабочем каталоге. 

Пример:
```bash
git mv oldname.txt newname.txt
```

---

#### git stash

Временное сохранение незавершённой работы:

```bash
git stash push -m "WIP"
git stash pop
```

---

#### git tag

`git tag` создаёт метку на коммите:

```bash
git tag v1.0.0
git tag -a v1.0.1 -m "Release"
```

---

#### git fetch

Загрузка изменений с remote без слияния:

```bash
git fetch origin
```

Слияние с локальной веткой — см. **`git pull`** выше.

---

#### git remote

Управление удалёнными репозиториями:

```bash
git remote -v
git remote add origin https://github.com/user/repo.git
```

---

#### git archive

`git archive` – создание архива текущего состояния репозитория. Пример:

```bash
git archive --format=zip --output=project.zip main
```

---

#### git submodule

`git submodule` – работа с подмодулями. Добавляет другие репозитории как подмодули текущего проекта.

```bash
git submodule
```

---

#### git show

Просмотр коммита, тега или объекта:

```bash
git show HEAD
git show v1.0.0
```

---

#### git shortlog

`git shortlog` – краткая история коммитов. Группирует коммиты по авторам.

```bash
git shortlog
```

---

#### git describe

`git describe` – описание текущего состояния. Показывает ближайший тег и количество коммитов после него.

---

#### git clone

Клонирование удалённого репозитория:

```bash
git clone https://github.com/user/repo.git
git clone --depth 1 https://github.com/user/repo.git
```

---

#### git config

Настройка Git (локально, глобально или для системы):

```bash
git config --global user.name "Имя"
git config --global user.email "you@example.com"
git config --list
```

---

#### git push --force-with-lease

Команда **git push --force-with-lease** — это безопасная альтернатива **git push --force**.

Она позволяет переписать историю удалённой ветки, но делает это с дополнительной проверкой, чтобы не затереть чужие изменения.

```bash
git push --force-with-lease
git push --force-with-lease origin feature/rebased-branch
```

То есть команда перезаписывает только если HEAD на сервере не изменился.

Когда использовать
— После `git rebase` или `git commit --amend`, когда история изменилась.

```bash
git commit --amend
```
— Когда работаете в команде и хотите сохранить чужие изменения.
— На pull request ветках — безопаснее, чем просто `--force`.

---