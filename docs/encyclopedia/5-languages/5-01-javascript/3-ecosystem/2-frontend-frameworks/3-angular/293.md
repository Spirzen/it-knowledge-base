---
tags: [engineer, developer, architector, required, beginner]
title: "Angular — DI, RxJS и формы"
description: "Внедрение зависимостей, сервисы, HttpClient, Observable и reactive forms."
slug: /encyclopedia/5-languages/5-01-javascript/293
---

# Angular — DI, RxJS и формы

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

---

## Маршрут Angular

| Шаг | Статья |
|-----|--------|
| 1 | [Первая программа](./292.md) |
| 2 | [Обзор](./29.md) |
| 3 | **Вы здесь** |

---

## Внедрение зависимостей (DI)

Сервис — класс с `@Injectable()`; Angular создаёт экземпляр и передаёт в конструктор:

```typescript
@Injectable({ providedIn: 'root' })
export class NotesService {
  constructor(private http: HttpClient) {}

  list() {
    return this.http.get<Note[]>('/api/notes');
  }
}
```

```typescript
@Component({ /* ... */ })
export class NotesComponent {
  notes$ = this.notesService.list();

  constructor(private notesService: NotesService) {}
}
```

`providedIn: 'root'` — один экземпляр на приложение.

---

## HttpClient и RxJS

HTTP-методы возвращают **Observable**, не Promise:

```typescript
this.http.get<Note[]>('http://127.0.0.1:3000/notes').subscribe({
  next: (data) => (this.notes = data),
  error: (err) => console.error(err),
});
```

В шаблоне с **async pipe** подписка закрывается автоматически:

```html
<li *ngFor="let note of notes$ | async">{{ note.text }}</li>
```

Основы RxJS: `map`, `filter`, `switchMap` (отмена старого запроса при новом поиске).

---

## Маршрутизация

```typescript
export const routes: Routes = [
  { path: '', component: HomeComponent },
  { path: 'notes', component: NotesComponent },
  { path: '**', component: NotFoundComponent },
];
```

```html
<a routerLink="/notes">Заметки</a>
<router-outlet />
```

---

## Формы

| Тип | Когда |
|-----|--------|
| Template-driven | Простые формы, `ngModel` |
| Reactive | Сложная валидация, `FormBuilder` |

Reactive example:

```typescript
form = this.fb.group({
  text: ['', [Validators.required, Validators.maxLength(2000)]],
});
```

```html
<form [formGroup]="form" (ngSubmit)="save()">
  <input formControlName="text" />
</form>
```

---

## Standalone-компоненты

Современный Angular без NgModule для каждой фичи:

```typescript
@Component({
  standalone: true,
  imports: [CommonModule, RouterLink],
  template: `...`,
})
export class NotesComponent {}
```

Практика — [Первая программа на Angular](./292.md).

---

## Связанные материалы

- [Angular](./29.md) — архитектура платформы
- [Справочник по Angular](./291.md) — справочник
- [TypeScript](../../../5-10-typescript/intro.md)
