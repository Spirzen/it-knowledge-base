---
tags: [engineer, developer, architector, required, beginner]
title: "Точка входа в Node.js — require.main и import.meta"
description: "Как отличить прямой запуск node script.js от import/require модуля в CommonJS и ESM; аналог if __name__ == \"__main__\" в Python."
slug: /encyclopedia/5-languages/5-01-javascript/48
related:
  - title: "Первая программа на Node.js"
    doc: encyclopedia/5-languages/5-01-javascript/3-ecosystem/1-runtime-node/262
  - title: "Node.js — серверный JavaScript"
    doc: encyclopedia/5-languages/5-01-javascript/3-ecosystem/1-runtime-node/26
  - title: "if __name__ == \"__main__\" — Python"
    doc: encyclopedia/5-languages/5-02-python/40
---

# Точка входа в Node.js — require.main и import.meta

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>

В Python есть `if __name__ == "__main__":`. В **Node.js** тот же вопрос звучит так: **файл запустили командой `node file.js` или подключили через `require` / `import`?**

См. также: [Первая программа на Node.js](./262.md) · [модули Node](./26.md) · [ESM в браузере](../../../40.md) · [Python — if __name__ == "__main__"](/encyclopedia/5-languages/5-02-python/40) · [карта Node.js](./intro.md).

---

## Запуск файла и подключение как модуль

Файл `calculator.js` экспортирует функции и внизу демонстрирует их:

```javascript
function add(a, b) {
  return a + b;
}

console.log(add(5, 3));
module.exports = { add };
```

Команда `node -e "require('./calculator')"` выведет `8`, хотя нужна была только функция `add`. Демо-код стоит выполнять **только при прямом запуске**.

---

## CommonJS — `require.main === module`

В модулях **CommonJS** (`require`, `module.exports`) Node задаёт:

| Объект | Смысл |
|--------|-------|
| `module` | Текущий модуль |
| `require.main` | Модуль, с которого начался процесс |
| `require.main === module` | **true**, если этот файл — точка входа |

```javascript
// calculator.cjs
function add(a, b) {
  return a + b;
}

module.exports = { add };

if (require.main === module) {
  console.log(add(5, 3));
}
```

| Способ | Поведение |
|--------|-----------|
| `node calculator.cjs` | Показывает демо |
| `require('./calculator.cjs')` | Только экспорт, без `console.log` |

Часто логику выносят в `main()` и вызывают её внутри проверки — как в Python.

---

## ESM — `import.meta.url` и `process.argv[1]`

В **ES-модулях** (`"type": "module"` в `package.json` или расширение `.mjs`) переменной `require.main` нет. Сравнивают путь запущенного файла с URL текущего модуля:

```javascript
// calculator.mjs

import { fileURLToPath } from 'node:url';

export function add(a, b) {
  return a + b;
}

const isMain =
  process.argv[1] === fileURLToPath(import.meta.url);

if (isMain) {
  console.log(add(5, 3));
}
```

- `import.meta.url` — URL текущего файла; по нему в ESM понимают, какой модуль выполняется.
- `process.argv[1]` — путь, который передали в `node …`.

<div class="callout callout--info">
  <div class="callout-title">Браузер и bundler</div>

  <div class="callout-body">
  В браузере скрипт подключают через <code>&lt;script type="module"&gt;</code>; отдельной точки входа в стиле CLI там нет. Паттерн с <code>require.main</code> и проверкой <code>import.meta.url</code> нужен в <strong>Node.js</strong>, утилитах и тестах.
</div>
</div>

---

## Когда писать проверку

| Ситуация | Нужна ли |
|----------|----------|
| Одноразовый скрипт в `npm run` | По желанию |
| Модуль с функциями + локальное демо | **Да** |
| Express-приложение в `app.js`, импортируемое в тестах | **Да** — `listen` только при прямом запуске файла |
| Только `export` без кода верхнего уровня | Часто достаточно без обёртки |

---

## Сравнение с Python

| | Python | Node.js (CJS) | Node.js (ESM) |
|---|--------|---------------|---------------|
| Проверка | `__name__ == "__main__"` | `require.main === module` | `process.argv[1]` и `import.meta.url` |
| Импорт выполняет код верхнего уровня | Да | Да | Да |

Общий обзор по языкам — в [Python — if __name__ == "__main__"](/encyclopedia/5-languages/5-02-python/40#v-drugih-yazykah).

---

## Связанные материалы

| Тема | Статья |
|------|--------|
| npm, `package.json` | [Первая программа на Node.js — Первая программа](./262.md) |
| CommonJS и ESM подробнее | [Node.js - серверный JavaScript — Node.js](./26.md) |
| `import.meta` в браузере | [40 — ESM](../../../40.md) |
| Карта блока Node | [intro](./intro.md) |
