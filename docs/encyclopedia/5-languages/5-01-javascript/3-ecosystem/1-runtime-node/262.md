---
tags: [engineer, developer, architector, required, beginner]
title: "Первая программа на Node.js"
description: "Node.js с нуля — консоль, npm, встроенный http и REST API на Express с пошаговым разбором для новичка."
slug: /encyclopedia/5-languages/5-01-javascript/262
related:
  - title: "Первая программа на TypeScript"
    doc: encyclopedia/5-languages/5-10-typescript/4
  - title: "Первая программа на Kotlin"
    doc: encyclopedia/5-languages/5-09-kotlin/2
  - title: "Первая программа на C++"
    doc: encyclopedia/5-languages/5-06-cpp/1002
  - title: "Первая программа на Ruby"
    doc: encyclopedia/5-languages/5-11-ruby/20
---

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


import ExternalPlayEmbed from '@site/src/components/ExternalPlayEmbed';


# Первая программа на Node.js

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

<ExternalPlayEmbed example="lab/first-program-play" title="Первая программа" minHeight={420} playProps={{ language: 'node' }} />

---

## Первая программа на Node.js

В браузере JavaScript ограничен правилами безопасности: нельзя слушать произвольный порт или читать файлы на диске без разрешения. **Node.js** запускает тот же язык как обычную программу на компьютере или сервере — с доступом к сети, файловой системе и процессам.

Практикум идёт **по шагам**: консоль и npm, встроенный `http`, затем **Express** и учебное REST API "Заметки".

| Шаг | Тема | Зачем |
|-----|------|-------|
| 1 | `node -v`, терминал | Убедиться, что среда готова |
| 2 | Файл `.js` и `node app.js` | Первая программа вне браузера ([схемы запуска](/encyclopedia/1-basics/1-12-sovety-dlya-novichka/13)) |
| 3 | REPL | Быстрые эксперименты в консоли |
| 4 | `process.argv`, `readline` | CLI-утилиты и ввод пользователя |
| 5 | `package.json`, `npm run` | Манифест проекта и скрипты |
| 6 | `http.createServer` | HTTP без фреймворка |
| 7 | Express REST API | Маршруты, JSON, middleware |

| Материал | Зачем |
|----------|-------|
| [Node.js — серверный JavaScript](./26.md) | Event loop, модули CJS/ESM |
| [npm](./265.md) | install, scripts, lock, audit |
| [Структура проекта](./266.md) | `src/`, env, конвенции |
| [fs и http](./268.md) | Встроенные модули без Express |
| [Точка входа — require.main](./48.md) | Прямой `node file.js` vs `import` |
| [Express — middleware](./263.md) | Router, CORS, ошибки |
| [Справочник по Node](./261.md) | Справочник API Node |
| [Fullstack](./264.md) | CORS, прокси, два порта |

### Навигация по блоку Node.js

- Вы здесь: [Первая программа на Node.js](./262.md)
- Устройство runtime: [Node.js - серверный JavaScript](./26.md)
- npm и структура: [npm — команды, зависимости и lock-файлы](./265.md), [Структура Node-проекта и правила разработки](./266.md)
- Следующий шаг: [Встроенные модули Node.js — fs, потоки и http](./268.md) → [Express — middleware, маршруты и ошибки](./263.md)
- Склейка с фронтендом: [Fullstack на JavaScript — API и фронтенд](./264.md)

<div class="callout callout--tip">
  <div class="callout-title">Редактор и терминал</div>

  <div class="callout-body">
  Команды удобно выполнять во вкладке <strong>Терминал</strong> VS Code (<code>Ctrl+`</code>). Отладка и работа с IDE — <a href="/encyclopedia/4-code-dev/4-14-razrabotka-i-otladka/111">статья «Отладка»</a>.
</div>
</div>

---

## Шаг 1 — проверка установки

Скачайте **LTS** с [nodejs.org](https://nodejs.org/) и проверьте версии:

```bash
node -v
npm -v
```

Команда `node -v` показывает версию runtime (для новых проектов берите LTS 20 или 22). `npm -v` — менеджер пакетов из той же установки. Если терминал пишет, что `node` не найден, проверьте `PATH` — см. [статью про Node.js](./26.md).

Создайте рабочую папку, например `notes-api`, и откройте её в редакторе — дальше все команды выполняются из неё.

---

## Шаг 2 — программа из файла

Файл `hello.js` (кодировка **UTF-8**):

```javascript
console.log('Привет, мир!');
```

Запуск:

```bash
node hello.js
```

Разбор:
- `node` передаёт файл интерпретатору V8; браузер здесь не участвует.
- Расширение `.js` в команде можно опустить: `node hello` тоже сработает.
- `console.log` пишет в **stdout** — стандартный поток вывода консоли.

<div class="callout callout--info">
  <div class="callout-title">Кодировка на Windows</div>

  <div class="callout-body">
  Сохраняйте исходники в UTF-8. Node.js сам преобразует текст при выводе в консоль; кириллица в <code>console.log</code> обычно отображается корректно.
</div>
</div>

---

## Шаг 3 — интерактивный режим (REPL)

Команда `node` **без аргументов** открывает REPL — read-eval-print loop:

```bash
node
```

```text
> 10 + 12 * 2
34
> console.log('test')
test
undefined
> .exit
```

Разбор:
- Каждое выражение выполняется сразу после Enter — удобно для калькулятора и проверки синтаксиса.
- `console.log` возвращает `undefined`, поэтому REPL печатает его на отдельной строке.
- Последний результат доступен в переменной `_` (одно подчёркивание).
- Выход: `.exit`, `Ctrl+D` или дважды `Ctrl+C`.

REPL подходит для коротких экспериментов; полноценные программы храните в файлах.

---

## Шаг 4 — аргументы командной строки и ввод с клавиатуры

### `process.argv`

При запуске `node script.js arg1 arg2` массив **`process.argv`** содержит путь к `node`, путь к скрипту и переданные аргументы:

```javascript
// argv-demo.js
console.log(process.argv);
```

```bash
node argv-demo.js --help version
```

Первые два элемента — служебные; пользовательские аргументы начинаются с индекса `2`. Значение с пробелами передавайте в кавычках: `node app.js "hello world"`.

### `readline` — диалог в консоли

Для интерактивного ввода после старта программы:

```javascript
// ask.js
import readline from 'node:readline/promises';
import { stdin as input, stdout as output } from 'node:process';

const rl = readline.createInterface({ input, output });
const name = await rl.question('Как вас зовут? ');
console.log(`Здравствуйте, ${name}!`);
await rl.close();
```

Разбор:
- `readline/promises` даёт `await rl.question()` вместо колбэков.
- `stdin` / `stdout` связывают интерфейс с консолью.
- `rl.close()` освобождает ресурсы; без этого процесс может не завершиться.

Более сложные CLI-примеры — в [простых приложениях на JavaScript](../../103.md).

### Завершение процесса

```javascript
process.exitCode = 1; // код ошибки для ОС (0 — успех)
// или немедленно:
process.exit(1);
```

Перед `process.exit()` дождитесь вывода в консоль: буфер stdout может не успеть сброситься.

---

## Шаг 5 — `package.json` и npm-скрипты

Превратите каталог в **npm-пакет**:

```bash
npm init -y
```

Флаг `-y` создаёт `package.json` с значениями по умолчанию. Основные поля:

| Поле | Смысл |
|------|--------|
| `name` | Имя пакета |
| `version` | SemVer (`1.0.0`) |
| `main` | Точка входа (`index.js`) |
| `scripts` | Команды для `npm run` |
| `dependencies` | Пакеты для runtime |
| `devDependencies` | Инструменты разработки |

Добавьте ESM и скрипт запуска:

```json
{
  "name": "notes-api",
  "type": "module",
  "scripts": {
    "hello": "node hello.js"
  }
}
```

```bash
npm run hello
```

Разбор:
- `"type": "module"` включает `import` / `export` (Node.js 18+).
- `npm run <имя>` ищет команду в `scripts` и добавляет в `PATH` каталог `node_modules/.bin`.
- `npm install <пакет>` записывает зависимость в `package.json`; `npm install` без аргументов восстанавливает всё из манифеста.

---

## Шаг 6 — HTTP-сервер на встроенном `http`

Express строится поверх модуля **`http`**. Полезно один раз собрать сервер без фреймворка — так видно, что Express добавляет поверх `createServer`.

Файл `server-raw.js`:



<ExternalCodeEmbed example="javascript/js-501-3-ecosystem-1-runtime-node-262-001" title="Шаг 6 — HTTP-сервер на встроенном `http`" minHeight={372} />



```bash
node server-raw.js
curl http://127.0.0.1:3000/health
```

Разбор:
- `http.createServer` принимает callback `(req, res)` на каждый запрос.
- `req.method` и `req.url` — метод HTTP и путь без хоста.
- `res.writeHead` задаёт статус и заголовки; `res.end` отправляет тело и закрывает ответ.
- Маршрутизацию, разбор JSON и middleware приходится писать вручную — Express берёт это на себя.

Подробный разбор `http`, потоков и `req`/`res` — во [встроенных модулях Node.js](./268.md).

---

## Шаг 7 — REST API на Express

Логичный шаг после синтаксиса JS и `async/await` — поднять **HTTP-сервер**, который отвечает JSON. **Express** даёт маршруты, middleware и удобные `req` / `res` поверх `http`.

Мы соберём **REST API "Заметки"** — тот же учебный сценарий, что в [Gin](/encyclopedia/5-languages/5-10-go/2412.md) и [Ktor](/encyclopedia/5-languages/5-09-kotlin/221.md). Данные пока в **памяти**: после перезапуска список пустой.

### Что такое REST в двух словах

**REST** — стиль API, где ресурсы имеют **URL**, а действие задаёт **HTTP-метод**:

| Метод | Обычный смысл | В нашем API |
|-------|----------------|-------------|
| GET | Прочитать | Список или одна заметка |
| POST | Создать | Новая заметка в теле JSON |
| DELETE | Удалить | Заметка по id в пути |

### Что получится

| Метод | Путь | Действие |
|-------|------|----------|
| GET | `/health` | Сервер жив |
| GET | `/notes` | Список заметок |
| POST | `/notes` | Тело `&#123; "text": "..." &#125;` — создать |
| DELETE | `/notes/:id` | Удалить по id |

---

### Инициализация проекта

```bash
npm install express
npm install -D nodemon
```

Обновите `package.json`:

```json
{
  "name": "notes-api",
  "type": "module",
  "scripts": {
    "dev": "nodemon server.js",
    "start": "node server.js"
  }
}
```

| Поле / пакет | Смысл |
|--------------|--------|
| `express` | Маршруты и middleware |
| `nodemon` | Перезапуск при сохранении файла |
| `npm run dev` | Разработка с watch |
| `npm start` | Обычный запуск |

---

### Минимальный сервер Express

Файл `server.js` — точка входа.

1. **`express()`** — объект приложения.
2. **`app.use(express.json())`** — разбор JSON-тела в `req.body`.
3. **Маршруты** — обработчики, которые срабатывают, когда совпали URL и HTTP-метод.
4. **`app.listen(PORT)`** — слушать порт (по умолчанию 3000).



<ExternalCodeEmbed example="javascript/js-501-3-ecosystem-1-runtime-node-262-002" title="Минимальный сервер Express" minHeight={720} />



Запуск: `npm run dev`. Остановка — `Ctrl+C`. Подробнее про dev-сервер и браузер — [Запуск и перезапуск приложений](/encyclopedia/1-basics/1-12-sovety-dlya-novichka/13).

---

### Разбор фрагментов

| Код | Смысл |
|-----|--------|
| `_req` | Параметр не используем, сигнатура нужна |
| `req.body?.text` | Optional chaining |
| `?? ''` | Замена `null`/`undefined` на пустую строку |
| `return res.status(400)...` | Прерывание обработчика |
| `201` | Created — ресурс создан |
| `req.params.id` | Часть URL `/notes/:id` |
| `204` | Успех без тела (DELETE) |
| `process.env.PORT` | Порт из окружения (хостинг) |

---

### Проверка через curl

```bash
curl http://127.0.0.1:3000/health
curl http://127.0.0.1:3000/notes
curl -X POST http://127.0.0.1:3000/notes \
  -H "Content-Type: application/json" \
  -d "{\"text\": \"Изучить Node\"}"
curl http://127.0.0.1:3000/notes
curl -X DELETE http://127.0.0.1:3000/notes/1
```

`Connection refused` — сервер не запущен. Пустой `req.body` — нет `express.json()` или заголовка `Content-Type: application/json`.

---

### Middleware — цепочка до маршрута

**Middleware** — функция `(req, res, next)`. Express вызывает их **по порядку** регистрации.

| Middleware | Задача |
|------------|--------|
| `express.json()` | Разобрать JSON-тело |
| Логирование | method, url, время |
| `cors` | Браузерный фронт с другого порта — [Express — middleware, маршруты и ошибки](./263.md) |

Пример логирования:

```javascript
app.use((req, res, next) => {
  const started = Date.now();
  res.on('finish', () => {
    console.log(req.method, req.url, res.statusCode, `${Date.now() - started}ms`);
  });
  next();
});
```

Подробнее: [Express 263](./263.md).

---

### Router — вынести маршруты

`routes/notes.js`:



<ExternalCodeEmbed example="javascript/js-501-3-ecosystem-1-runtime-node-262-003" title="Router — вынести маршруты" minHeight={480} />



В `server.js`: `app.use('/notes', createNotesRouter(store));` — `router.get('/')` + префикс `/notes` = **`GET /notes`**.

---

### Частые ошибки

| Симптом | Причина | Что сделать |
|---------|---------|-------------|
| `node` не найден | Node не в `PATH` | Переустановить или добавить путь — [Node.js - серверный JavaScript](./26.md) |
| `Cannot find module 'express'` | Нет `npm install` | `npm install` в каталоге проекта |
| `req.body` undefined | Нет `express.json()` | Middleware выше маршрутов; curl с `-H Content-Type` |
| DELETE не находит id | Неверный шаблон пути | `/notes/:id`, читать `req.params.id` |
| Изменения не видны | Запущен `node`, не nodemon | `npm run dev` |
| CORS в браузере | Фронт :5173, API :3000 | [Express — middleware, маршруты и ошибки](./263.md) или [Fullstack на JavaScript — API и фронтенд](./264.md) |

---

### Что попробовать дальше

1. Центральный `errorHandler` — [Express — middleware, маршруты и ошибки](./263.md).
2. SQLite / Prisma — заметки переживут перезапуск.
3. React + `fetch` — [272](../../272.md), [Fullstack на JavaScript — API и фронтенд](./264.md), [Fetch / axios — типовые запросы](/lab/Примеры/1145), [галерея компонентов](/lab/Примеры/1146) (блок [список с API](/lab/Примеры/1146#fetch-list)).
4. Тесты supertest — [Тестирование JavaScript — Vitest и Testing Library](../../33.md).

---

## Как превратить учебный API в рабочий сервис

### План эволюции

1. In-memory store для отладки маршрутов.
2. Подключение БД и миграций.
3. Единый формат ошибок и валидация входных данных.
4. Тесты на ключевые сценарии.
5. Логи, метрики и контролируемое завершение процесса.

### Базовые контракты API

| Эндпоинт | Успех | Ошибка клиента | Ошибка сервера |
|---|---|---|---|
| `GET /notes` | `200` + массив | `400` при неверных параметрах | `500` |
| `POST /notes` | `201` + объект | `400` при пустом `text` | `500` |
| `DELETE /notes/:id` | `204` | `404`, если id не найден | `500` |

---

## Связанные материалы

| Тема | Материал |
|------|----------|
| Express углублённо | [Express — middleware, маршруты и ошибки](./263.md) |
| Fullstack | [Fullstack на JavaScript — API и фронтенд](./264.md) |
| TypeScript на сервере | [TypeScript](../../30.md) |
| Теория Node | [Node.js - серверный JavaScript](./26.md) |

<div class="callout callout--warning">
  <div class="callout-title">Production</div>

  <div class="callout-body">
  Для боевого сервера: process manager (PM2, systemd), секреты в переменных окружения, HTTPS за reverse proxy (Nginx, Caddy).

  Пример `proxy_pass` и заголовков — [Nginx — конфиги под задачу](/lab/Примеры/11112).

  Данные в памяти теряются при перезапуске — нужна БД и бэкапы.
</div>
</div>

<div class="callout callout--tip">
  <div class="callout-title">Основа по протоколу</div>

  <div class="callout-body">
  Базовый разбор HTTP и HTTPS — <a href="/encyclopedia/2-system-network/2-09-osnovy-integratsionnogo-vzaimodeystviya/118">HTTP как основа веб-интеграций</a>.
</div>
</div>

{/* sidebar-collections */}

---

## В подборках

Статья входит в [тематические подборки](/about/collections) и блок «С чего начать?» на [главной](/). Соседние шаги того же маршрута:

**Первые шаги** ([маршрут подборки](/about/collections/pervye-shagi)) — [Первая программа на TypeScript](/encyclopedia/5-languages/5-10-typescript/4), [Первая программа на Kotlin](/encyclopedia/5-languages/5-09-kotlin/2), [Первая программа на C++](/encyclopedia/5-languages/5-06-cpp/1002), [Первая программа на Ruby](/encyclopedia/5-languages/5-11-ruby/20), [Первая программа на C#](/encyclopedia/5-languages/5-05-csharp/16), [Первая программа на Rust](/encyclopedia/5-languages/5-13-rust/20).

{/* /sidebar-collections */}

---
