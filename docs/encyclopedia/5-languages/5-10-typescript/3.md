---
tags: [engineer, developer, architector, required, beginner]
title: "Экосистема и архитектура TypeScript"
description: "Компилятор, DefinitelyTyped, ESLint, монорепозиторий, project references, CI typecheck и типичные стеки frontend/backend."
related:
  - title: "Форматы и подключение"
    doc: encyclopedia/5-languages/5-10-typescript/9
  - title: "TypeScript Server"
    doc: encyclopedia/5-languages/5-10-typescript/16
  - title: "Рекомендации"
    doc: encyclopedia/5-languages/5-10-typescript/6
  - title: "Архитектура компиляции"
    doc: encyclopedia/5-languages/5-10-typescript/15
  - title: "Обзор TypeScript"
    doc: encyclopedia/5-languages/5-01-javascript/30
  - title: "Справочник tsconfig"
    doc: encyclopedia/5-languages/5-01-javascript/301
---

# Экосистема и архитектура TypeScript

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

**Дальше:** [Форматы и подключение](./9.md) · [TypeScript Server](./16.md) · [JS-курс: monorepo и публикация](/encyclopedia/5-languages/5-01-javascript/30#monorepo-и-публикация) · [Справочник — tsconfig](/encyclopedia/5-languages/5-01-javascript/301#11-конфигурация-typescript-tsconfigjson)

---

TypeScript в продакшене — не только `tsc`: **пакетный менеджер**, типы из `@types/*`, **сборщик** (Vite, esbuild), **линтер**, тесты и **CI**. Здесь — карта экосистемы, структура репозитория, **монорепозиторий** с project references и проверка типов в пайплайне.

**Маршрут:** [О разделе](./intro.md) → **экосистема** → [Форматы и подключение](./9.md) → [Компиляция](./15.md).

> Обзор без учебных деталей — [Обзор TypeScript в JS](/encyclopedia/5-languages/5-01-javascript/30). Таблицы `tsconfig` — [Справочник по TypeScript](/encyclopedia/5-languages/5-01-javascript/301).

---

## Слои экосистемы

```text
  Исходники .ts / .tsx
        │
        ├── tsserver (IDE, tsserver)
        ├── tsc / tsc -b (проверка, emit, declarations)
        ├── bundler (Vite, esbuild, Webpack) — часто без emit tsc
        ├── ESLint (@typescript-eslint)
        └── CI: typecheck + test + build
        ▼
  JavaScript → Node / браузер
```

| Компонент | Роль |
|-----------|------|
| **typescript** | компилятор, `tsc`, tsserver |
| **@types/\*** | типы для JS-библиотек (DefinitelyTyped) |
| **Bundler** | сборка для браузера, tree-shaking |
| **ESLint + Prettier** | стиль и часть логических правил |
| **Vitest / Jest** | тесты с TS из коробки |

---

## Типичная структура одного пакета

```text
my-app/
├── package.json
├── tsconfig.json
├── src/
│   ├── index.ts
│   └── types/
│       └── api.ts
├── dist/              # после tsc (в .gitignore)
└── tests/
```

`package.json` (фрагмент):

```json
{
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "@types/node": "^22.0.0"
  }
}
```

Разбор:

- Типы API выносят в `src/types` — контракт между слоями — [Рекомендации по разработке на TypeScript](./6.md).
- `typecheck` в CI даже если bundle делает Vite — [Архитектура компиляции TypeScript и runtime](./15.md).

---

## DefinitelyTyped и `@types`

Библиотеки на чистом JS не несут `.d.ts` — типы ставят отдельно:

```bash
npm i lodash
npm i -D @types/lodash
```

Разбор:

- Пакеты `@types/*` публикуются из [DefinitelyTyped](https://github.com/DefinitelyTyped/DefinitelyTyped).
- Современные пакеты иногда включают типы в npm (`"types"` в `package.json`) — `@types` не нужен.

---

## Рекомендуемый стартовый стек

| Инструмент | Назначение |
|------------|------------|
| TypeScript + `strict` | проверка типов |
| ESLint `@typescript-eslint` | правила TS, unused vars |
| Prettier | форматирование |
| Vitest или Jest | unit-тесты |
| `tsc --noEmit` в CI | gate на PR |

Минимальный `tsconfig` — [Форматы и подключение TypeScript](./9.md); политика strict — [Рекомендации по разработке на TypeScript](./6.md).

---

## Frontend vs backend

| Направление | Сборка | Типичный `module` |
|-------------|--------|-------------------|
| **SPA (React/Vue)** | Vite | `ESNext` + bundler resolution |
| **Node API** | `tsc` или `tsx` | `NodeNext` |
| **Библиотека npm** | `tsc` + `declaration` | `NodeNext` / `ESM` |

Прикладные статьи: [TypeScript и React](./21.md) (React), [TypeScript и Node.js](./22.md) (Node).

---

## Монорепозиторий

Несколько пакетов в одном git-репозитории — **monorepo** (pnpm workspaces, npm workspaces, Nx, Turborepo).

```text
repo/
├── package.json          # workspaces
├── tsconfig.base.json
├── packages/
│   ├── core/
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   └── src/
│   └── app/
│       ├── package.json
│       ├── tsconfig.json
│       └── src/
```

Корневой `tsconfig.base.json`:

```json
{
  "compilerOptions": {
    "composite": true,
    "declaration": true,
    "declarationMap": true,
    "strict": true,
    "module": "NodeNext",
    "moduleResolution": "NodeNext"
  }
}
```

`packages/core/tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}
```

`packages/app/tsconfig.json` с зависимостью:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src"
  },
  "references": [{ "path": "../core" }],
  "include": ["src"]
}
```

Сборка всего дерева:

```bash
tsc -b
```

Разбор:

- **`composite: true`** — пакет можно подключать через `references`.
- **`tsc -b`** (build mode) собирает граф в правильном порядке и **инкрементально**.
- **tsserver** использует тот же граф — быстрее в IDE на больших репо — [TypeScript Server](./16.md).

Указатель в JS-курсе — [JS-курс: monorepo и публикация](/encyclopedia/5-languages/5-01-javascript/30#monorepo-и-публикация). Таблицы — [Справочник — tsconfig](/encyclopedia/5-languages/5-01-javascript/301#11-конфигурация-typescript-tsconfigjson).

---

## Публикация библиотеки на npm

| Шаг | Действие |
|-----|----------|
| 1 | `"declaration": true` — генерировать `.d.ts` |
| 2 | `"types": "dist/index.d.ts"`, `"exports"` в `package.json` |
| 3 | `"files": ["dist"]` — только артефакты |
| 4 | `npm publish` после `tsc -b` |

Потребители получают **типы вместе с JS** — контракт API стабилен.

---

## CI: проверка типов

Пример шага GitHub Actions:

```yaml
- uses: actions/checkout@v4
- uses: actions/setup-node@v4
  with:
    node-version: "22"
    cache: "npm"
- run: npm ci
- run: npm run typecheck
```

Для monorepo:

```yaml
- run: npx tsc -b
```

Разбор:

- `typecheck` должен падать PR при ошибке типов — тот же контракт, что в IDE.
- Не полагайтесь только на `build` bundler'а: он может **пропустить** отдельные файлы.

Кэширование: `node_modules` + incremental `.tsbuildinfo` при `composite`.

---

## Разделение ответственности: tsc vs bundler

| Задача | Кто |
|--------|-----|
| Проверка типов всего проекта | `tsc --noEmit` или `tsc -b` |
| Быстрый dev-server | Vite |
| Транспиляция в старый target | `tsc` или esbuild в bundler |
| `.d.ts` для библиотеки | `tsc` с `declaration` |

Частый паттерн SPA: **Vite собирает**, **`tsc -b` или `tsc --noEmit`** проверяет типы (в шаблоне `react-ts` это шаг `build` перед `vite build`) — [Архитектура компиляции TypeScript и runtime](./15.md). Для Vue-проектов аналог — `vue-tsc --noEmit`.

---

## Связь с разделом энциклопедии

| Слой | Где |
|------|-----|
| Учёба | `5-10-typescript/*.md` |
| Обзор в JS-курсе | [TypeScript](/encyclopedia/5-languages/5-01-javascript/30) |
| Таблицы | [Справочник по TypeScript](/encyclopedia/5-languages/5-01-javascript/301) |
| Индекс | [Справочник по TypeScript](./2.md) — не второй справочник |

---

## Частые ошибки

| Ошибка | Причина | Что делать |
|--------|---------|------------|
| Нет `typecheck` в CI | полагались на Vite | `tsc --noEmit` |
| Версии TS расходятся | глобальный tsserver | workspace TS — [TypeScript Server](./16.md) |
| Monorepo тормозит | один tsconfig на всё | `references` + `composite` |
| Типы не находятся | нет `@types` | установить или обновить пакет |
| `dist` в git | артефакты в репо | `.gitignore`, `files` в publish |
| Дублирование 301 в статьях | шум | ссылка на справочник |

---

## Практика

1. Добавьте в учебный проект скрипт `typecheck` и шаг в CI (или локальный `npm run typecheck` перед push).
2. Вынесите типы API в `src/types` и импортируйте в сервисе.
3. Нарисуйте схему: IDE → tsserver → тот же `tsconfig`, что и CI.
4. (Опционально) Создайте два пакета `core` + `app` с `references` и соберите `tsc -b`.
5. Установите `@types` для одной JS-библиотеки и проверьте автодополнение.


---

## Смежные статьи

- [Подключение](./9.md) — ESM, `tsconfig`
- [Компиляция](./15.md) — emit, source maps
- [TypeScript Server](./16.md) — IDE
- [Рекомендации](./6.md) — strict, Zod, миграция
- [Первая программа](./4.md) — старт с нуля
- [Справочник](./2.md) — [TypeScript](/encyclopedia/5-languages/5-01-javascript/30), [Справочник по TypeScript](/encyclopedia/5-languages/5-01-javascript/301)
