---
tags: [engineer, developer, architector, required, beginner]
title: "TypeScript и React"
description: "Типизация props и state, хуки, события форм, LoadState в компонентах и слой типов API в React + TypeScript."
related:
  - title: "Асинхронность в TypeScript"
    doc: encyclopedia/5-languages/5-10-typescript/17
  - title: "События и обработка событий"
    doc: encyclopedia/5-languages/5-10-typescript/20
  - title: "Обработка ошибок"
    doc: encyclopedia/5-languages/5-10-typescript/27
  - title: "Справочник React"
    doc: encyclopedia/5-languages/5-01-javascript/271
  - title: "Простые приложения"
    doc: encyclopedia/5-languages/5-10-typescript/5
  - title: "Справочник — React в 301"
    doc: encyclopedia/5-languages/5-01-javascript/301
---

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


# TypeScript и React

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

**Дальше:** [TypeScript и Node.js](./22.md) · [События](./20.md) · [Асинхронность](./17.md) · [Справочник — интеграция с фреймворками](/encyclopedia/5-languages/5-01-javascript/301#14-интеграция-с-фреймворками)

---

React описывает UI как **функции от состояния**. TypeScript добавляет **контракты**: какие `props` обязательны, что лежит в `state`, какие поля пришли с API. Это снижает поломки при рефакторинге компонентов и согласует фронт с backend-DTO.

**Маршрут:** [Типы и типизация](./10.md) → [Асинхронность](./17.md) → **React** → [обработка ошибок](./27.md).

> Курс по React: [справочник 271](/encyclopedia/5-languages/5-01-javascript/271). Старт проекта — [Первая программа на TypeScript](./4.md) + `npm create vite@latest -- --template react-ts`.

---

## Интеграция с фреймворками (обзор)

Эта статья — **React**; общая картина [Справочник — интеграция с фреймворками](/encyclopedia/5-languages/5-01-javascript/301#14-интеграция-с-фреймворками):

| Стек | Шаблон / типы | Учебный материал |
|------|---------------|------------------|
| **React** | `react-ts`, `@types/react` | **эта статья**, [Справочник по React](/encyclopedia/5-languages/5-01-javascript/271) |
| **Vue** | `vue-tsc`, `defineComponent` | [Справочник по Vue.js](/encyclopedia/5-languages/5-01-javascript/281) |
| **Angular** | встроенный TS, strict templates | [Справочник по Angular](/encyclopedia/5-languages/5-01-javascript/291) |
| **Node.js** | `@types/node`, DTO API | [TypeScript и Node.js](./22.md) |

Общие принципы для всех: типизировать **props/inputs**, **ответы API**, **состояния загрузки** (discriminated union) — [Типы данных и типизация в TypeScript](./10.md), [Асинхронное программирование в TypeScript](./17.md).

---

## TypeScript в React-проектах

| Без TS | С TS |
|--------|------|
| Опечатка в `props` — в runtime | Ошибка при сборке / в IDE |
| Неверное поле от API | Подсветка при доступе |
| Рефакторинг prop name | Find references + компилятор |
| `useState(null)` — любой тип | `useState<User \| null>(null)` |

TypeScript **не заменяет** тесты и не валидирует JSON с сервера сам — для API нужны guards или Zod — [Рекомендации по разработке на TypeScript](./6.md), [Асинхронное программирование в TypeScript](./17.md).

---

## Старт: Vite + React + TS

```bash
npm create vite@latest my-app -- --template react-ts
cd my-app && npm install && npm run dev
```

Разбор:

- Шаблон даёт `tsconfig.app.json`, типы React, ESLint.
- Сборку делает Vite; типы проверяют **`tsc -b`** (в `package.json` часто в скрипте `build` перед `vite build`). Отдельно для CI удобно `tsc --noEmit`.

Структура типов:

```text
src/
├── components/
├── types/
│   └── api.ts      # DTO и LoadState
└── App.tsx
```

---

## Компонент и `props`



<ExternalCodeEmbed example="typescript/typescript-510-21-001" title="Компонент и `props`" minHeight={336} />



Разбор:

- **`children?: React.ReactNode`** — текст, элемент, массив, `null`.
- Не обязательно `React.FC` — явный тип props у функции читаемее; в [Справочник по TypeScript](/encyclopedia/5-languages/5-01-javascript/301#14-интеграция-с-фреймворками) описаны варианты.

### Расширение props нативного элемента

```tsx
type ButtonProps = {
  label: string;
} & React.ButtonHTMLAttributes<HTMLButtonElement>;

function Button({ label, ...rest }: ButtonProps) {
  return <button type="button" {...rest}>{label}</button>;
}
```

Разбор:

- `...rest` пробрасывает `onClick`, `disabled`, `className` с проверкой типов.

---

## Типизация UI-компонентов

Сниппеты из [Uiverse / Galaxy](/lab/Примеры/1155) — это HTML+CSS. В React/Vue те же блоки оформляют **props** и **discriminated unions**, чтобы варианты кнопки, поля и переключателя не смешивались.



<ExternalCodeEmbed example="typescript/typescript-510-21-002" title="Типизация UI-компонентов" minHeight={588} />



Поле ввода с ошибкой с сервера:



<ExternalCodeEmbed example="typescript/typescript-510-21-003" title="Типизация UI-компонентов" minHeight={534} />



Тип переключателя (checkbox vs radio vs switch) — union, чтобы не перепутать API:

```tsx
type ToggleControl =
  | { kind: 'checkbox'; checked: boolean; onChange: (v: boolean) => void; label: string }
  | { kind: 'radio'; name: string; value: string; checked: boolean; label: string }
  | { kind: 'switch'; checked: boolean; onChange: (v: boolean) => void; label: string };
```

Стили из [Типовых элементов интерфейса](/encyclopedia/3-data-markup/3-10-css/113) подключают через CSS Modules или глобальный класс; логика остаётся в TypeScript.

Готовые файлы `Button.tsx`, `ProfileCard.tsx`, `ThemeSwitch.tsx`, `Tooltip.tsx` с разбором — [React — UI на TypeScript](/lab/Примеры/1146#ui-typescript).

---

## `useState` с явным типом

```tsx
type User = { id: string; name: string };

function UserBadge() {
  const [user, setUser] = React.useState<User | null>(null);

  if (!user) return <p>Не авторизован</p>;
  return <span>{user.name}</span>;
}
```

Разбор:

- `useState(null)` без generic в strict даёт только `null` — укажите `User | null`.
- Обновление: `setUser({ id: "1", name: "Ann" })` проверяется на соответствие `User`.

---

## LoadState в компоненте

Тот же discriminated union, что в [Асинхронное программирование в TypeScript](./17.md):



<ExternalCodeEmbed example="typescript/typescript-510-21-004" title="LoadState в компоненте" minHeight={720} />



Разбор:

- `cancelled` в cleanup — защита от setState после размонтирования.
- `switch` по `status` сужает тип без лишних флагов `isLoading`.

---

## `useReducer` с типизированными actions

Когда состояние и переходы сложнее, чем один `setState`, удобен **discriminated union** для действий:



<ExternalCodeEmbed example="typescript/typescript-510-21-005" title="`useReducer` с типизированными actions" minHeight={720} />



Разбор:

- `CartAction` — union с полем `type`; в ветке `switch` доступны только нужные поля (`sku` в `add`).
- `dispatch({ type: "addd", sku: "x" })` — ошибка компиляции (опечатка в `type`).
- Тот же приём, что для `LoadState`, но для **событий UI** — [Паттерны в TypeScript](./28.md).

---

## `ComponentProps` — переиспользование props

Не дублируйте поля нативного элемента — возьмите тип у готового компонента или HTML-тега:



<ExternalCodeEmbed example="typescript/typescript-510-21-006" title="`ComponentProps` — переиспользование props" minHeight={318} />



Разбор:

- `ComponentProps<"button">` — все допустимые props `<button>` с типами событий.
- `Pick` оставляет в публичном API только нужные поля обёртки.

---

## События форм



<ExternalCodeEmbed example="typescript/typescript-510-21-007" title="События форм" minHeight={408} />



| Тип события | Элемент |
|-------------|---------|
| `ChangeEvent<HTMLInputElement>` | `<input>` |
| `ChangeEvent<HTMLSelectElement>` | `<select>` |
| `FormEvent<HTMLFormElement>` | `<form onSubmit>` |
| `MouseEvent<HTMLButtonElement>` | `<button onClick>` |

Подробнее DOM — [События и обработка событий в TypeScript](./20.md).

---

## `useRef` и DOM

```tsx
function FocusInput() {
  const ref = React.useRef<HTMLInputElement>(null);

  const focus = () => ref.current?.focus();

  return (
    <>
      <input ref={ref} />
      <button type="button" onClick={focus}>Фокус</button>
    </>
  );
}
```

Разбор:

- `ref.current` имеет тип `HTMLInputElement | null` — нужен optional chaining.

---

## Контекст с типом



<ExternalCodeEmbed example="typescript/typescript-510-21-008" title="Контекст с типом" minHeight={318} />



Разбор:

- `null` по умолчанию заставляет проверять провайдер в хуке.
- Альтернатива — значение по умолчанию в `createContext` (менее строго).

---

## Слой типов API

`src/types/api.ts`:

```ts
export type UserDto = { id: string; name: string; email: string };
export type CreateUserBody = Pick<UserDto, "name" | "email">;
```

Компонент импортирует **только типы**:

```tsx
import type { UserDto } from "../types/api.js";
```

Разбор:

- DTO ≠ domain: на границе сети — отдельные типы — [Рекомендации по разработке на TypeScript](./6.md), [TypeScript и Node.js](./22.md).
- `import type` не попадает в JS-бандл.

---

## Дженерики в компонентах



<ExternalCodeEmbed example="typescript/typescript-510-21-009" title="Дженерики в компонентах" minHeight={336} />



См. [Дженерики в TypeScript](./24.md).

---

## Частые ошибки

| Ошибка | Что делать |
|--------|------------|
| `props: any` | явный `type Props` |
| `as User` на `json()` | `unknown` + guard |
| Разрозненные `loading`/`error` | `LoadState` union |
| Забыли `children` в типе | `React.ReactNode` |
| `React.FC` с implicit `children` | явные props (стиль команды) |
| Типы API в каждом файле | `types/api.ts` |

---

## Практика

1. Создайте `react-ts` проект и компонент `Card` с typed props.
2. Реализуйте `Profile` с `LoadState` и `isUser` guard.
3. Форма с `onSubmit` и `ChangeEvent<HTMLInputElement>`.
4. Вынесите `UserDto` в `types/api.ts`, используйте `import type`.
5. Добавьте generic `List<T>` для массива задач.
6. Перепишите локальное состояние корзины на `useReducer` с union actions.


---

## Смежные статьи

- [Асинхронность](./17.md) · [События](./20.md) · [Ошибки](./27.md)
- [Простые приложения](./5.md) · [Паттерны](./28.md)
- [Синтаксис](./8.md) — `.tsx`, `import type`
- [Справочник по React](/encyclopedia/5-languages/5-01-javascript/271) · [Справочник — интеграция с фреймворками](/encyclopedia/5-languages/5-01-javascript/301#14-интеграция-с-фреймворками)
