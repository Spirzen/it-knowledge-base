---
tags: [engineer, developer, architector, required, beginner]
title: "Обработка ошибок в TypeScript"
description: "throw, Result, Option, unknown в catch, доменные ошибки, HTTP-маппинг и Zod на границе API."
related:
  - title: "Асинхронность"
    doc: encyclopedia/5-languages/5-10-typescript/17
  - title: "Рекомендации"
    doc: encyclopedia/5-languages/5-10-typescript/6
  - title: "TypeScript и Node.js"
    doc: encyclopedia/5-languages/5-10-typescript/22
  - title: "Паттерны"
    doc: encyclopedia/5-languages/5-10-typescript/28
  - title: "Справочник — паттерны"
    doc: encyclopedia/5-languages/5-01-javascript/301
---

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


# Обработка ошибок в TypeScript

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

**Дальше:** [Паттерны](./28.md) · [TypeScript и Node.js](./22.md) · [Асинхронность](./17.md) · [Справочник — распространённые паттерны](/encyclopedia/5-languages/5-01-javascript/301#15-распространённые-паттерны)

<div class="callout callout--info">
  <div class="callout-title">Сначала — общая теория</div>

  <div class="callout-body">
  <a href="/encyclopedia/4-code-dev/4-06-arhitektura-vypolneniya/111">Ошибки, исключения и отказоустойчивость</a> — <a href="/encyclopedia/4-code-dev/4-06-arhitektura-vypolneniya/111#oshibka-i-isklyuchenie">ошибка и исключение</a>.<br />
  Синтаксис <code>throw</code> и <code>try/catch</code> в рантайме — [Обработка исключений в JavaScript](/encyclopedia/5-languages/5-01-javascript/242).
</div>
</div>

---

Механика `throw` в TypeScript та же, что в JavaScript. Зато типы помогают **описать ошибки явно**: ожидаемые сбои удобно класть в `Result` (discriminated union успех/ошибка), инфраструктурные — обрабатывать в `catch` с `unknown`. Так важные ветки обработки видны в контракте, без потери в общем `try/catch`. Удобный порядок — [асинхронность](./17.md), эта статья, [паттерны](./28.md), [TypeScript и Node.js](./22.md).

> `unknown` в catch — [Типы данных и типизация в TypeScript](./10.md), [Асинхронное программирование в TypeScript](./17.md). Валидация — [Рекомендации по разработке на TypeScript](./6.md).

---

## Два семейства ошибок

| Тип | Примеры | Как моделировать |
|-----|---------|------------------|
| **Ожидаемые** | невалидный email, ресурс не найден | `Result`, union, HTTP 4xx |
| **Неожиданные** | диск, OOM, баг | `throw`, HTTP 5xx, лог + alert |

```ts
// Ожидаемая — часть контракта
function parseAge(input: string): Result<number, "nan" | "negative"> { /* ... */ }

// Неожиданная — прерывает поток
async function readSecret(): Promise<string> {
  const data = await fs.readFile("/secret", "utf8");
  return data;
}
```

---

## `Result<T, E>`



<ExternalCodeEmbed example="typescript/typescript-510-27-001" title="`Result<T, E>`" minHeight={390} />



Разбор:

- Вызывающий **обязан** проверить `ok` — иначе нет доступа к `value`.
- `E` можно сузить до union литералов — автодополнение в `if (!r.ok)`.

### Комбинирование Result

```ts
function mapResult<T, U, E>(
  r: Result<T, E>,
  fn: (v: T) => U,
): Result<U, E> {
  return r.ok ? { ok: true, value: fn(r.value) } : r;
}
```

В библиотеках: `neverthrow`, `ts-results` — по желанию команды.

---

## `Option<T>` (отсутствие значения)



<ExternalCodeEmbed example="typescript/typescript-510-27-002" title="`Option<T>` (отсутствие значения)" minHeight={318} />



Разбор:

- Альтернатива — `T | null` с `strictNullChecks` — [Переменные и константы в TypeScript](./11.md).
- `Option` явно показывает в API, что значения может не быть.

---

## `try/catch` и `unknown`



<ExternalCodeEmbed example="typescript/typescript-510-27-003" title="`try/catch` и `unknown`" minHeight={354} />



Разбор:

- Не используйте `catch (e: any)`.
- `instanceof Error` не покрывает все `throw` в JS.

---

## Кастомные классы ошибок

```ts
class AppError extends Error {
  constructor(
    message: string,
    readonly code: "NOT_FOUND" | "FORBIDDEN" | "VALIDATION",
  ) {
    super(message);
    this.name = "AppError";
  }
}

function isAppError(e: unknown): e is AppError {
  return e instanceof AppError;
}
```

HTTP-маппинг в Node:

```ts
function toStatus(e: AppError): number {
  switch (e.code) {
    case "NOT_FOUND":
      return 404;
    case "FORBIDDEN":
      return 403;
    case "VALIDATION":
      return 400;
  }
}
```

Разбор:

- Класс удобен для **центрального** error middleware.
- Для чистых функций предпочтительнее `Result` без throw.

---

## Ошибки в async-цепочках

```ts
async function fetchUser(id: string): Promise<Result<User, "http" | "invalid">> {
  try {
    const res = await fetch(`/api/users/${id}`);
    if (!res.ok) return { ok: false, error: "http" };
    const raw: unknown = await res.json();
    if (!isUser(raw)) return { ok: false, error: "invalid" };
    return { ok: true, value: raw };
  } catch {
    return { ok: false, error: "http" };
  }
}
```

`Promise` **reject** и возврат `Result` — выберите один стиль на слой; смешение усложняет код.

---

## Единый формат API-ошибки



<ExternalCodeEmbed example="typescript/typescript-510-27-004" title="Единый формат API-ошибки" minHeight={354} />



Разбор:

- Frontend типизирует `ApiErrorBody` и показывает `details` у полей формы.

---

## Zod на границе



<ExternalCodeEmbed example="typescript/typescript-510-27-005" title="Zod на границе" minHeight={336} />



Подробнее — [Рекомендации по разработке на TypeScript](./6.md).

---

## throw и Result — шпаргалка

| Ситуация | Рекомендация |
|----------|--------------|
| Валидация формы / DTO | `Result` или Zod `safeParse` |
| Ресурс не найден в сервисе | `Result` или `AppError` NOT_FOUND |
| Падение диска / сеть без обработки | `throw` + middleware |
| React load data | union `LoadState` — [TypeScript и React](./21.md) |

---

## Частые ошибки

| Ошибка | Что делать |
|--------|------------|
| `catch (e: any)` | `unknown` + narrowing |
| Игнорировать `!r.ok` | включить ESLint на floating promises / strict |
| throw для валидации везде | Result на границе domain |
| Один `Error` на всё | union кодов или классы |
| Типы ошибок не в API | `ApiErrorBody` |

---

## Практика

1. Перепишите парсер числа на `Result<number, "nan" | "negative">`.
2. Добавьте `findById` с `Option` или `Result`.
3. Сделайте `AppError` + маппинг в HTTP-статус.
4. В React отобразите `LoadState` error — [TypeScript и React](./21.md).
5. Оберните `POST` body в Zod `safeParse` (опционально).


---

## Смежные статьи

- [Async](./17.md) · [Рекомендации](./6.md) · [TypeScript и Node.js](./22.md) · [Паттерны](./28.md)
- [Справочник — распространённые паттерны](/encyclopedia/5-languages/5-01-javascript/301#15-распространённые-паттерны)
