---
tags: [engineer, developer, architector]
title: "TypeORM"
description: "Сущности, репозитории, миграции и связи в TypeORM с TypeScript — обзор для Node backend."
related:
  - title: "TypeScript и Node.js"
    doc: encyclopedia/5-languages/5-10-typescript/22
  - title: "Декораторы"
    doc: encyclopedia/5-languages/5-10-typescript/23
  - title: "Объекты и классы"
    doc: encyclopedia/5-languages/5-10-typescript/18
  - title: "Обработка ошибок"
    doc: encyclopedia/5-languages/5-10-typescript/27
---

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


# TypeORM

<div class="article-tags">
  <span class="tag tag-advanced">ПРОДВИНУТОЕ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

**Дальше:** [TypeScript и Node.js](./22.md) · [Декораторы](./23.md) · [Обработка ошибок](./27.md)

---

**TypeORM** — ORM для TypeScript/JavaScript: таблицы описывают **классами-сущностями**, запросы — через **Repository** и QueryBuilder. Типизация полей сущности проверяется TS; SQL и связи задаются декораторами или схемой Data API.

**Маршрут:** [TypeScript и Node.js](./22.md) → **TypeORM**. Нужны [декораторы](./23.md) и `experimentalDecorators` в legacy-стеке.

> Альтернативы: Prisma (schema-first), Drizzle (SQL-like types). TypeORM выбран как распространённый в NestJS-стеке.

---

## Что даёт ORM

| Без ORM | С TypeORM |
|---------|-----------|
| SQL строки вручную | методы репозитория |
| Расхождение полей и таблицы | класс `Entity` |
| Миграции отдельно | CLI миграций (при настройке) |

TS **не заменяет** миграции и индексы — схему БД проектируют осознанно.

---

## Минимальная сущность

```ts
import { Entity, PrimaryGeneratedColumn, Column } from "typeorm";

@Entity("users")
export class User {
  @PrimaryGeneratedColumn("uuid")
  id!: string;

  @Column({ unique: true })
  email!: string;

  @Column()
  name!: string;
}
```

Разбор:

- `@Entity("users")` — имя таблицы.
- `!` — поле инициализирует БД/ORM, не конструктор — [Декораторы в TypeScript](./23.md).

---

## Инициализация DataSource



<ExternalCodeEmbed example="typescript/typescript-510-26-001" title="Инициализация DataSource" minHeight={354} />



Разбор:

- **`synchronize: true`** удобно в dev, опасно в prod — используйте **миграции**.
- Секреты — из env — [TypeScript и Node.js](./22.md).

---

## Repository

```ts
const repo = AppDataSource.getRepository(User);

async function createUser(email: string, name: string): Promise<User> {
  const user = repo.create({ email, name });
  return repo.save(user);
}

async function findUser(id: string): Promise<User | null> {
  return repo.findOneBy({ id });
}
```

Разбор:

- `findOneBy` → `User | null` — обработайте null — [Обработка ошибок в TypeScript](./27.md).
- `create` + `save` — два шага; можно `insert` для bulk.

---

## Связь one-to-many



<ExternalCodeEmbed example="typescript/typescript-510-26-002" title="Связь one-to-many" minHeight={426} />



Разбор:

- `() => User` — ленивая ссылка на класс (циклический import).
- Загрузка связей: `relations: { posts: true }` в `find`.

---

## DTO vs Entity

```ts
type CreateUserDto = { email: string; name: string };

async function register(dto: CreateUserDto): Promise<User> {
  return createUser(dto.email, dto.name);
}
```

Не экспортируйте `Entity` напрямую в API-ответ — маппер `toDto(user)` — [TypeScript и Node.js](./22.md), [Паттерны в TypeScript](./28.md).

---

## Миграции

```bash
npx typeorm migration:generate -d src/data-source.ts src/migrations/AddUserIndex
npx typeorm migration:run -d src/data-source.ts
```

Разбор:

- Миграция — версионированное изменение схемы.
- Генерация сравнивает entities с БД — проверяйте SQL вручную.

---

## Слой сервиса

```ts
export class UserService {
  constructor(private readonly repo: Repository<User>) {}

  async getOrThrow(id: string): Promise<User> {
    const user = await this.repo.findOneBy({ id });
    if (!user) throw new AppError(`User ${id}`, "NOT_FOUND");
    return user;
  }
}
```

Разделение: **controller** → **service** → **repository** — [TypeScript и Node.js](./22.md).

---

## Частые ошибки

| Ошибка | Что делать |
|--------|------------|
| `synchronize: true` в prod | миграции |
| Entity в JSON ответе | DTO |
| N+1 запросов | `relations` или join |
| Нет `reflect-metadata` | import в entry |
| `any` в QueryBuilder | типизировать параметры |

---

## Практика

1. Поднимите Postgres (Docker) и сущность `User`.
2. Добавьте `Post` с `ManyToOne` / `OneToMany`.
3. Сгенерируйте и примените миграцию.
4. Сервис `getOrThrow` с доменной ошибкой.
5. Ответ API через DTO, не `User` entity.


---

## Смежные статьи

- [TypeScript и Node.js](./22.md) · [Декораторы](./23.md) · [Ошибки](./27.md) · [Паттерны](./28.md)
