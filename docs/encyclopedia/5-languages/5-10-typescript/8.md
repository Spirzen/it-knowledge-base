---
tags: [engineer, developer, architector, required, beginner]
title: "Синтаксис и пунктуация TypeScript"
description: "Аннотации типов, interface vs type, import type, .ts и .tsx, пунктуация и связь со справочником 301."
related:
  - title: "Типы данных и типизация"
    doc: encyclopedia/5-languages/5-10-typescript/10
  - title: "Функции в TypeScript"
    doc: encyclopedia/5-languages/5-10-typescript/14
  - title: "Объекты и классы"
    doc: encyclopedia/5-languages/5-10-typescript/18
  - title: "Синтаксис JavaScript"
    doc: encyclopedia/5-languages/5-01-javascript/12
  - title: "Справочник — интерфейсы"
    doc: encyclopedia/5-languages/5-01-javascript/301
  - title: "Знаки препинания и символы в IT"
    doc: encyclopedia/1-basics/1-30-angliyskiy-yazyk/7
---

# Синтаксис и пунктуация TypeScript

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>

**Дальше:** [Типы и типизация](./10.md) · [Функции](./14.md) · [Объекты и классы](./18.md) · [Справочник — интерфейсы](/encyclopedia/5-languages/5-01-javascript/301#5-интерфейсы)

---

TypeScript — это **JavaScript плюс система типов**. Синтаксис выполнения (скобки, запятые, `function`) совпадает с JS; сверху идут аннотации ` — T`, `interface`, `type`, дженерики `<T>`. В статье — базовая грамматика TS и выбор между `interface` и `type`; полные таблицы — в [Справочник по TypeScript](/encyclopedia/5-languages/5-01-javascript/301).

**Маршрут:** [Первая программа](./4.md) → **синтаксис** → [Типы и типизация](./10.md) → [функции](./14.md).

> База JS: [синтаксис JavaScript](/encyclopedia/5-languages/5-01-javascript/12). Таблицы конструкций — [Справочник — интерфейсы](/encyclopedia/5-languages/5-01-javascript/301#5-интерфейсы). **Имена символов:** [Знаки препинания и символы в IT](/encyclopedia/1-basics/1-30-angliyskiy-yazyk/7).

---

## Что добавляет TypeScript к JavaScript

```ts
type User = {
  id: string;
  name: string;
};

function printUser(user: User): void {
  console.log(user.name);
}
```

| Элемент | Пример | Где в рантайме |
|---------|--------|----------------|
| Аннотация параметра | `user: User` | исчезает |
| Возврат функции | `: void` | исчезает |
| `type` / `interface` | `User` | исчезает |
| `import type` | только типы | исчезает |

В `.js` после `tsc` остаётся обычный JavaScript — [Архитектура компиляции TypeScript и runtime](./15.md).

---

## Пунктуация: JS + типы

| Символ | В JS | В TS дополнительно |
|--------|------|-------------------|
| `:` | метки в объектах `{ a: 1 }` | аннотация `name: string` |
| `\|` | — | union `A \| B` |
| `&` | — | intersection `A & B` |
| `<>` | — | дженерики `Array<T>`, JSX в `.tsx` |
| `?` | optional chaining `?.` | optional property `email?` |
| `!` | логическое НЕ | non-null assertion `x!` (осторожно) |
| `...` | spread/rest | то же + в типах rest tuple |
| `;` | часто опционален | то же |

Точка с запятой между полями `interface` зависит от стиля команды; ASI из JS сохраняется. Знак `?` после имени поля означает, что свойство может отсутствовать: `email?: string` эквивалентно `email: string | undefined`.

---

## Объявление типов: `type`

```ts
type Id = string;
type Status = "idle" | "loading" | "done";
type Point = { x: number; y: number };
type Handler = (event: MouseEvent) => void;
```

Ключевое слово `type` задаёт **алиас**, то есть новое имя для типа. Union литералов вроде `Status` — основа discriminated unions; подробнее в [Операторы и условные ветвления в TypeScript](./12.md).

---

## Объявление контрактов: `interface`

```ts
interface Person {
  readonly id: string;
  name: string;
  email?: string;
}

interface Employee extends Person {
  department: string;
}
```

Разбор:

- `extends` у интерфейсов — объединение полей.
- `interface` можно **дополнять** (declaration merging) — полезно для `.d.ts` библиотек, редко в прикладном коде.

Полный список возможностей интерфейса (индексные сигнатуры, callable) — [Справочник — интерфейсы](/encyclopedia/5-languages/5-01-javascript/301#5-интерфейсы).

---

## `interface` vs `type`: когда что

| Задача | Предпочтение |
|--------|--------------|
| Форма объекта, публичный API | `interface` |
| Union, tuple, mapped types | `type` |
| Расширение объекта | `interface extends` или `type` + `&` |
| Примитивный алиас `type Id = string` | только `type` |

```ts
interface Animal {
  name: string;
}

type Pet = Animal & {
  ownerId: string;
};

type Result = { ok: true; data: string } | { ok: false; error: string };
```

Разбор:

- Для **React props** и DTO часто `interface`; для **результата операции** — `type` с union.
- В [Объекты и классы в TypeScript](./18.md) класс может `implements interface`.

<div class="callout callout--info">
  <div class="callout-title">Правило команды</div>

  <div class="callout-body">
  Важнее единообразие в команде, чем бесконечный спор interface vs type. Зафиксируйте стиль в ESLint / code review — <a href="./6.md">рекомендации</a>.
</div>
</div>

---

## Пересечение типов (`&`)

```ts
type Timestamps = {
  createdAt: Date;
  updatedAt: Date;
};

type User = {
  id: string;
  name: string;
} & Timestamps;
```

Разбор:

- `&` объединяет **все** поля; конфликтующие несовместимые свойства дадут `never`.
- Для объектов часто читаемее один `interface` с `extends`.

---

## Импорт только типов: `import type`

```ts

import type { User } from "./models.js";
import { createUser, type UserDto } from "./api.js";

export type { User };
```

Разбор:

- `import type` гарантирует: импорт **удалится** при компиляции — нет лишних зависимостей в JS.
- Смешанный импорт `value + type` — TypeScript 4.5+.
- В runtime-модулях путь часто с `.js` при `NodeNext` — [Форматы и подключение TypeScript](./9.md).

---

## Экспорт типов

```ts
// types.ts
export interface Config {
  port: number;
}

export type LogLevel = "info" | "warn" | "error";
```

Потребитель:

```ts

import type { Config, LogLevel } from "./types.js";

```

---

## Файлы `.ts`, `.tsx`, `.d.ts`

| Расширение | Содержимое |
|------------|------------|
| `.ts` | TypeScript без JSX |
| `.tsx` | TypeScript + JSX (React и др.) |
| `.d.ts` | только объявления типов (библиотеки) |

```tsx
// Button.tsx
type Props = { label: string; onClick: () => void };

export function Button({ label, onClick }: Props) {
  return <button type="button" onClick={onClick}>{label}</button>;
}
```

Разбор:

- В `.tsx` generic пишут как `const List = <T,>(items: T[]) => ...` с запятой после `T`, чтобы не путать с JSX.
- Прикладной React — [TypeScript и React](./21.md).

---

## Аннотации в функциях

```ts
function sum(a: number, b: number): number {
  return a + b;
}

type BinaryOp = (a: number, b: number) => number;
const mul: BinaryOp = (a, b) => a * b;
```

Подробно — [Функции в TypeScript](./14.md); перегрузки и `this` там же.

---

## `satisfies` и синтаксис объектов

```ts
type Routes = Record<string, { path: string; private?: boolean }>;

const routes = {
  home: { path: "/" },
  admin: { path: "/admin", private: true },
} satisfies Routes;
```

Оператор `satisfies` проверяет форму, **не расширяя** вывод полей — см. [Типы данных и типизация в TypeScript](./10.md). Аннотация `: Routes` проще, но теряет узкий тип вроде `home.path`.

---

## Комментарии и JSDoc

TypeScript понимает **JSDoc** в `.js` файлах при `checkJs` / `allowJs`:

```js
/**
 * @param {string} name
 * @returns {string}
 */
function greet(name) {
  return `Hi, ${name}`;
}
```

В `.ts` предпочитайте нативные аннотации; JSDoc — для миграции — [Рекомендации по разработке на TypeScript](./6.md).

---

## Частые ошибки

| Ошибка | Причина | Что делать |
|--------|---------|------------|
| Путаница `:` в объекте и типе | один символ, разный смысл | значение ` { x: 1 }`, тип `{ x: number }` |
| Union в `interface` | нельзя напрямую | `type` или `extends` нескольких |
| Импорт типа как значения | runtime crash | `import type` |
| `!` вместо проверки | небезопасно | narrowing — [Операторы и условные ветвления в TypeScript](./12.md) |
| Generic в `.tsx` без запятой | парсер JSX | `<T,>` |
| Дублировать 301 в статье | шум | ссылка на справочник |

---

## Практика

1. Опишите `Config` через `interface`, расширьте `AppConfig extends Config` полем `appName`.
2. Опишите `ApiResponse` как `type` union `success | error`.
3. Разнесите импорт: значения из `./api.js`, типы через `import type`.
4. Перепишите объект маршрутов с `satisfies` и проверьте автодополнение в IDE.
5. Создайте `.tsx` с компонентом и typed props.


---

## Смежные статьи

- [Типы](./10.md) — примитивы, utility types
- [Функции](./14.md) — сигнатуры, overloads
- [Классы](./18.md) — `implements`
- [Подключение](./9.md) — модули, `NodeNext`
- [Справочник](./2.md) — [Справочник — интерфейсы и модули](/encyclopedia/5-languages/5-01-javascript/301#5-интерфейсы)
- JS: [Переменные в JavaScript](/encyclopedia/5-languages/5-01-javascript/17)
