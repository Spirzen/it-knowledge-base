---
tags: [engineer, developer, architector]
title: "Генераторы и итераторы в TypeScript"
description: "Generator<T>, Iterable, AsyncGenerator, for await...of и типизация ленивых последовательностей."
related:
  - title: "Асинхронность"
    doc: encyclopedia/5-languages/5-10-typescript/17
  - title: "Дженерики"
    doc: encyclopedia/5-languages/5-10-typescript/24
  - title: "Циклы"
    doc: encyclopedia/5-languages/5-10-typescript/13
  - title: "Итераторы в JavaScript"
    doc: encyclopedia/5-languages/5-01-javascript/23
---

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


# Генераторы и итераторы в TypeScript

<div class="article-tags">
  <span class="tag tag-advanced">ПРОДВИНУТОЕ</span>
</div>

<span class="complexity-badge">Разработчику</span>

**Дальше:** [Асинхронность](./17.md) · [Дженерики](./24.md) · [Циклы](./13.md)

---

**Генераторы** (`function*`) возвращают ленивую последовательность через `yield`. TypeScript типизирует их как `Generator<T>` и `AsyncGenerator<T>`. Полезно для потоков данных, пагинации и обхода без загрузки всего массива в память. Удобный порядок — [циклы](./13.md), [асинхронность](./17.md), эта статья.

> Runtime и протокол итерации — [итераторы в JavaScript](/encyclopedia/5-languages/5-01-javascript/23), [циклы в TypeScript](./13.md).

---

## `Generator<T>`

```ts
function* range(from: number, to: number): Generator<number> {
  for (let i = from; i <= to; i += 1) {
    yield i;
  }
}

for (const n of range(1, 3)) {
  console.log(n);
}
```

Разбор:

- `function*` возвращает **итерируемый** объект с методом `next()`.
- Тип yield — `number`; return type генератора — `Generator<number, void, unknown>` (третий параметр — тип `yield*`).

### Явный тип возврата

```ts
function* ids(): Generator<string, void, undefined> {
  let n = 0;
  while (true) {
    yield `id-${n += 1}`;
  }
}
```

---

## `Iterable` и `Symbol.iterator`

```ts
class Counter implements Iterable<number> {
  constructor(private readonly max: number) {}

  *[Symbol.iterator](): Generator<number> {
    for (let i = 1; i <= this.max; i += 1) {
      yield i;
    }
  }
}

for (const n of new Counter(3)) {
  console.log(n);
}
```

Разбор:

- `for...of` вызывает `Symbol.iterator`.
- Можно реализовать итератор вручную (`next(): IteratorResult<T>`), генератор проще.

---

## Делегирование — `yield*`

```ts
function* concat(
  a: Iterable<number>,
  b: Iterable<number>,
): Generator<number> {
  yield* a;
  yield* b;
}
```

Разбор:

- `yield*` перебирает другую последовательность.

---

## `AsyncGenerator` и `for await...of`



<ExternalCodeEmbed example="typescript/typescript-510-25-001" title="`AsyncGenerator` и `for await...of`" minHeight={480} />



Разбор:

- Генератор отдаёт **по одному** `User`, хотя API отвечает страницами — память не забивается всем списком сразу.
- `AsyncGenerator<User>` — каждый `yield` после `await` внутри `async function*`.
- Для production JSON лучше `unknown` + guard — [Асинхронное программирование в TypeScript](./17.md), [Рекомендации по разработке на TypeScript](./6.md); здесь `as Page<User>` для краткости.
- Тот же цикл в [Циклы в TypeScript](./13.md) — связка `for await...of` с постраничным API.

---

## Типизация `next` и ранний выход

```ts
function* gen(): Generator<number> {
  yield 1;
  yield 2;
  return 99;
}

const g = gen();
console.log(g.next()); // { value: 1, done: false }
console.log(g.return(0)); // завершение
```

Разбор:

- `return` в генераторе даёт `{ done: true, value: 99 }`.
- В прикладном коде чаще используют `break` в `for...of`.

---

## Когда использовать

| Сценарий | Подход |
|----------|--------|
| Ленивый обход большого списка | `function*` |
| Пагинация API | `async function*` |
| Простой массив | `for...of` / `map` — [Коллекции и массивы в TypeScript](./19.md) |
| RxJS-потоки | Observable (вне TS core) |

---

## Частые ошибки

| Ошибка | Что делать |
|--------|------------|
| Забыли `*` в `function*` | синтаксическая ошибка |
| Путаница `Generator` и `Iterable` | реализуйте `Symbol.iterator` |
| Бесконечный генератор без `break` | условие выхода |
| `for...of` на async gen | только `for await...of` |

---

## Практика

1. Напишите `range(from, to): Generator<number>`.
2. Реализуйте `Counter` как `Iterable`.
3. Сделайте `async function*` с тремя страницами mock API.
4. Сравните память: `[...range(1, 1_000_000)]` и цикл с `break` на 100.
5. Типизируйте возврат генератора явно.


---

## Смежные статьи

- [Async](./17.md) · [Дженерики](./24.md) · [Циклы](./13.md) · [Коллекции](./19.md)
