---
tags: [engineer, developer, architector, required]
title: "Паттерны в TypeScript"
description: "Discriminated union, branded types, mixins, фабрики, DTO-мапперы, type-safe events и exhaustive switch в прикладном TS."
related:
  - title: "Типы данных"
    doc: encyclopedia/5-languages/5-10-typescript/10
  - title: "Объекты и классы"
    doc: encyclopedia/5-languages/5-10-typescript/18
  - title: "Дженерики"
    doc: encyclopedia/5-languages/5-10-typescript/24
  - title: "Обработка ошибок"
    doc: encyclopedia/5-languages/5-10-typescript/27
  - title: "Справочник — паттерны"
    doc: encyclopedia/5-languages/5-01-javascript/301
---

import ExternalPlayEmbed from '@site/src/components/ExternalPlayEmbed';

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


# Паттерны в TypeScript

<ExternalPlayEmbed example="languages/pattern-matching-play" title="Pattern matching" minHeight={420} />


<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

**Дальше:** [Дженерики](./24.md) · [Обработка ошибок](./27.md) · [Объекты и классы](./18.md) · [Справочник — распространённые паттерны](/encyclopedia/5-languages/5-01-javascript/301#15-распространённые-паттерны)

Паттерны проектирования в TypeScript усиливаются **типами**: компилятор проверяет исчерпывающие `switch`, контракты фабрик и форму DTO. Ниже — приёмы, которые чаще всего встречаются в frontend и Node, без формального каталога GoF. Теория паттернов — [парадигмы и уровни абстракции](/encyclopedia/4-code-dev/4-07-paradigmy-i-urovni-abstraktsii/intro). Удобный порядок — [типы и типизация](./10.md), [классы](./18.md), эта статья, [обработка ошибок](./27.md). Таблицы — [Справочник — распространённые паттерны](/encyclopedia/5-languages/5-01-javascript/301#15-распространённые-паттерны).

---

## Discriminated union (команды и состояния)



<ExternalCodeEmbed example="typescript/typescript-510-28-001" title="Discriminated union (команды и состояния)" minHeight={462} />



Разбор:

- Поле `type` — дискриминант; в ветке доступны только нужные поля.
- `never` в `default` — страховка при расширении union — [Операторы и условные ветвления в TypeScript](./12.md).

Применение — Redux actions, очереди сообщений, UI state — [Асинхронное программирование в TypeScript](./17.md), [TypeScript и React](./21.md).

---

## Branded types (номинальность поверх structural)

```ts
type UserId = string & { readonly __brand: "UserId" };
type OrderId = string & { readonly __brand: "OrderId" };

function userId(raw: string): UserId {
  return raw as UserId;
}

function loadOrder(id: OrderId): void {
  console.log(id);
}

const u = userId("u-1");
loadOrder(u); // ошибка: OrderId ≠ UserId
```

Разбор:

- Оба в runtime — `string`, но TS **запрещает** перепутать.
- Валидацию формата (UUID) делайте в функции-конструкторе `userId`.

---

## Factory по интерфейсу



<ExternalCodeEmbed example="typescript/typescript-510-28-002" title="Factory по интерфейсу" minHeight={534} />



Разбор:

- Код зависит от `Logger`, без привязки к конкретному классу — проще тесты (mock).
- Structural typing: любой объект с `log` подойдёт, если не нужна номинальность.

---

## DTO + mapper между слоями



<ExternalCodeEmbed example="typescript/typescript-510-28-003" title="DTO + mapper между слоями" minHeight={390} />



Разбор:

- API (snake_case, string date) ≠ domain (`Date`).
- Один mapper на границе — не размазывать преобразования — [TypeScript и Node.js](./22.md).

---

## Type-safe event bus



<ExternalCodeEmbed example="typescript/typescript-510-28-004" title="Type-safe event bus" minHeight={720} />



Разбор:

- `K extends keyof Events` связывает **имя события** и **форму payload** — тот же приём, что у [updater](./24.md#паттерн-type-safe-updater): первый аргумент сужает второй.
- **Literal union** в `currency` и `role` ловит опечатки и неверные коды до запуска.
- В колбэке `on` доступны только поля выбранного события; деструктуризация чужого поля — ошибка компиляции.
- В продакшене — typed wrappers над EventEmitter или фреймворк.
- Тот же механизм `PayloadMap[E]` для отдельных handler-функций и констант `as const` — [дженерики §сопоставленные типы](./24.md#сопоставленные-типы-и-обобщённая-индексация).

---

## Builder с цепочкой



<ExternalCodeEmbed example="typescript/typescript-510-28-005" title="Builder с цепочкой" minHeight={372} />



Разбор:

- `this` return type сохраняет fluent API в подклассах.
- Для SQL в продакшене — query builder библиотеки, без ручных строк.

---

## Repository (интерфейс + реализация)



<ExternalCodeEmbed example="typescript/typescript-510-28-006" title="Repository (интерфейс + реализация)" minHeight={354} />



Связь с [Объекты и классы в TypeScript](./18.md), [TypeORM](./26.md).

---

## Примеси (mixins)

В TypeScript нет ключевого слова `mixin`, но **примесь** — это функция, которая принимает класс-конструктор и возвращает расширенный класс. Так комбинируют поведение без глубокого наследования.



<ExternalCodeEmbed example="typescript/typescript-510-28-007" title="Примеси (mixins)" minHeight={552} />



Разбор:

- `Timestamped` и `Identifiable` — **примеси**; порядок обёртки влияет на цепочку `super`.
- Типы параметров конструктора в реальных проектах сужают через `Constructor` и дженерики — [Дженерики в TypeScript](./24.md).
- В React чаще используют **композицию хуков**; классовые mixins — реже. В Nest/ORM — декораторы и базовые классы — [Декораторы в TypeScript](./23.md).

Альтернатива примесям для объектов без классов: пересечение типов + spread (`{ ...a, ...b }`) или отдельные функции-хелперы — [18.md §композиция](./18.md).

---

## Anti-patterns

| Плохо | Лучше |
|-------|--------|
| `any` на границе модуля | `unknown` + mapper |
| Строковые коды ошибок без union | `type ErrorCode = "..." \| "..."` |
| God-object type на 50 полей | разбить DTO / domain |
| `as` везде | guards, Zod |
| Дублировать union вручную | `as const` + `typeof` — [Типы данных и типизация в TypeScript](./10.md) |

---

## Частые ошибки

| Ошибка | Что делать |
|--------|------------|
| Union без дискриминанта | добавить поле `type` / `kind` |
| Забыли `never` в switch | exhaustive check |
| Branded type без factory | один вход `userId()` |
| Mapper в UI-компоненте | слой `api/` или `mappers/` |

---

## Практика

1. Опишите 3 команды `Command` и `handleCommand` с `never`.
2. Введите `UserId` / `OrderId` branded и поймайте ошибку присваивания.
3. Сделайте `fromDto` / `toDto` для одной сущности.
4. Добавьте `TypedEmitter` с двумя событиями.
5. Вынесите `Repository` interface и in-memory реализацию.


---

## Смежные статьи

- [Типы](./10.md) · [Дженерики](./24.md) · [Ошибки](./27.md) · [Классы](./18.md)
- [Справочник — распространённые паттерны](/encyclopedia/5-languages/5-01-javascript/301#15-распространённые-паттерны)
