---
tags: [engineer, developer, architector, required, beginner]
title: "if __FILE__ == $0 — запуск скрипта и require"
description: "Ruby-аналог if __name__ == \"__main__\": — когда файл запущен как ruby script.rb, а когда подключён через require."
related:
  - title: "Первая программа на Ruby"
    doc: encyclopedia/5-languages/5-11-ruby/20
  - title: "if __name__ == \"__main__\" — Python"
    doc: encyclopedia/5-languages/5-02-python/40
---

# if __FILE__ == $0 — запуск скрипта и require

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>

В Ruby ближайший аналог Python **`if __name__ == "__main__":`** — проверка **`if __FILE__ == $0`**.

| Переменная | Смысл |
|------------|-------|
| **`__FILE__`** | Путь к текущему файлу |
| **`$0`** | Имя скрипта, переданного интерпретатору (точка входа процесса) |

См. также: [Первая программа](./20.md) · [Python — разбор `__name__`](/encyclopedia/5-languages/5-02-python/40).

---

## Пример

```ruby
# calculator.rb
def add(a, b)
  a + b
end

if __FILE__ == $0
  puts add(5, 3)
end
```

**Прямой запуск:**

```bash
ruby calculator.rb
# => 8
```

**Подключение:**

```ruby
require_relative "calculator"
# демо не печатается
puts add(10, 20)
```

---

## require и код верхнего уровня

`require` / `require_relative` **выполняют** файл один раз (кэш в `$LOADED_FEATURES`). Любой код **вне** блока `if __FILE__ == $0` выполнится при первом `require`.

```ruby
puts "Файл загружен"   # выполнится при require

if __FILE__ == $0
  puts "Запуск как скрипт"
end
```

Демо и CLI держат внутри проверки; константы и определения методов — снаружи.

---

## Rails и гемы

В **Rails** точка входа — `bin/rails`, `config.ru`, а не `if __FILE__ == $0` в каждой модели. В **гемах** исполняемые команды регистрируют в `.gemspec` (`executables`).

Для учебных `.rb` и утилит паттерн с `$0` остаётся стандартом.

---

## Сравнение с Python

| Python | Ruby |
|--------|------|
| `__name__ == "__main__"` | `__FILE__ == $0` |
| `import module` | `require "module"` |

---

## Дальше по разделу

| Тема | Статья |
|------|--------|
| IRB, первая программа | [20 — Первая программа](./20.md) |
| Модули и классы | [102 — ООП](./102.md) |
| Rails | [21 — Rails](./21.md) |
