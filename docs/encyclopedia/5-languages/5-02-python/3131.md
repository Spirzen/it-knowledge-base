---
tags: [engineer, developer, architector, required, beginner]
title: Первая программа на PyQt6
description: Окно на PyQt6 — QApplication, виджеты, сигналы и слоты, разбор конвертера температуры.
---

# Первая программа на PyQt6

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

## Первая программа на PyQt6

### Где применяют PyQt6

**Desktop-приложение** — программа с окнами, кнопками и полями ввода на компьютере (в отличие от сайта в браузере).

**Tkinter** ([3111.md](./3111.md)) уже в стандартной библиотеке Python, но для «взрослого» desktop-UI часто берут **Qt** — богатые виджеты, стили, Designer, один код на Windows/macOS/Linux. **PyQt6** — привязки Qt 6 к Python. Почти тот же API у **PySide6** (лицензия LGPL от Qt); в коде замените `PyQt6` → `PySide6`.

Сравнение стеков: [PyQt, PySide и Flet](./313.md). Аналог на C++: [Qt — первая программа](/encyclopedia/5-languages/5-06-cpp/2731).

---

## Словарь Qt

| Термин | Простыми словами |
|--------|------------------|
| **Виджет (Widget)** | Элемент UI: кнопка, поле ввода, надпись |
| **Окно** | Контейнер, в котором лежат виджеты |
| **Layout** | Как виджеты выстраиваются (столбик, сетка) |
| **Сигнал (signal)** | Событие: «кнопку нажали», «текст изменился» |
| **Слот (slot)** | Функция, которая вызывается при сигнале |
| **Event loop** | Цикл «ждать действия пользователя и перерисовать» |

В Tkinter кнопку связывают так: `command=self.convert`. В Qt связь явная: `btn.clicked.connect(self.convert)` — удобно, когда сигналов много.

---

### Что получится

Окно **«Конвертер °C → °F»**: поле ввода, кнопка «Перевести», метка результата, диалог при неверном вводе. Формула: **°F = °C × 9/5 + 32**.

---

### Установка

```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# macOS / Linux:
source venv/bin/activate

pip install PyQt6
```

**venv** изолирует PyQt6 от других проектов на том же компьютере.

---

### Событийный цикл

GUI-программа не «заканчивается» после `show()`, как консольный скрипт:

1. `QApplication(sys.argv)` — инициализация подсистемы GUI.
2. Создать окно, вызвать `show()`.
3. `app.exec()` — цикл: клик → слот → перерисовка.

```python
app = QApplication(sys.argv)
window = MainWindow()
window.show()
sys.exit(app.exec())
```

---

### Код с разбором

`converter.py`:

```python
import sys

from PyQt6.QtWidgets import (
    QApplication,
    QLabel,
    QLineEdit,
    QMainWindow,
    QMessageBox,
    QPushButton,
    QVBoxLayout,
    QWidget,
)


class MainWindow(QMainWindow):
    def __init__(self) -> None:
        super().__init__()
        self.setWindowTitle("Конвертер °C → °F")

        self.input = QLineEdit()
        self.input.setPlaceholderText("Температура в °C")

        self.result = QLabel("—")

        btn = QPushButton("Перевести")
        btn.clicked.connect(self.convert)

        layout = QVBoxLayout()
        layout.addWidget(self.input)
        layout.addWidget(btn)
        layout.addWidget(self.result)

        container = QWidget()
        container.setLayout(layout)
        self.setCentralWidget(container)

    def convert(self) -> None:
        text = self.input.text().strip().replace(",", ".")
        try:
            c = float(text)
        except ValueError:
            QMessageBox.warning(self, "Ошибка", "Введите число")
            return
        f = c * 9 / 5 + 32
        self.result.setText(f"{c:.1f} °C = {f:.1f} °F")


def main() -> None:
    app = QApplication(sys.argv)
    window = MainWindow()
    window.resize(320, 160)
    window.show()
    sys.exit(app.exec())


if __name__ == "__main__":
    main()
```

### Разбор по блокам

| Элемент | Смысл |
|---------|--------|
| `QMainWindow` | Главное окно; здесь — central widget по центру |
| `QLineEdit` | Однострочный ввод; `setPlaceholderText` — серая подсказка |
| `btn.clicked.connect(self.convert)` | Сигнал → слот |
| `QVBoxLayout` | Виджеты столбиком |
| `container` + `setCentralWidget` | Layout вешают на виджет, виджет — в окно |
| `.replace(",", ".")` | Ввод `21,5` как в русской локали |
| `QMessageBox.warning` | Модальный диалог ошибки |

---

### Запуск

```bash
python converter.py
```

---

### Qt Designer (опционально)

Визуальный редактор сохраняет `.ui` (XML):

```python
from PyQt6 import uic
self.ui = uic.loadUi("mainwindow.ui", self)
```

Логику оставляют в Python.

---

### Частые ошибки

| Симптом | Причина | Решение |
|---------|---------|---------|
| Окно мелькает | Нет `app.exec()` | Добавить цикл событий |
| `ModuleNotFoundError: PyQt6` | venv не активирован | `pip install PyQt6` |
| Кнопка не работает | Нет `connect` | `btn.clicked.connect(...)` |

---

### Что попробовать

1. `QDialog` с формулой °F = °C × 9/5 + 32.
2. `QDoubleValidator` — только числа в поле.
3. Тот же UI в **PySide6** — [313.md](./313.md).

---

### Что попробовать

1. Второе окно `QDialog` с подсказкой по формуле °F = °C × 9/5 + 32.
2. `QLineEdit.setValidator` — только числа в поле ввода.
3. Тот же UI в **PySide6**: замените импорты и сравните лицензию в [313.md](./313.md).

---

### Что попробовать

1. Второе окно `QDialog` с подсказкой по формуле °F = °C × 9/5 + 32.
2. `QLineEdit.setValidator` — только числа в поле ввода.
3. Тот же UI в **PySide6**: замените импорты и сравните лицензию в [313.md](./313.md).

---

### Дальше

- `QTableWidget`, фоновые задачи через `QThread`;
- упаковка: PyInstaller;
- игры: [Разработка игр на Python](./312.md).

---
