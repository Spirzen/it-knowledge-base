---
tags: [analytic, tester, manager, required, beginner]
title: "Практикум Java — JUnit и REST Assured"
description: "Первый автотест на Java для QA: JUnit 5, REST Assured и проверка HTTP API без браузера."
---

# Практикум Java — JUnit и REST Assured

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Тестировщику</span>
<span class="complexity-badge">Разработчику</span>

> **Практикум №5 (Java).** После [pytest-практикума](./1011.md) — тот же цикл **Arrange → Act → Assert**, но в экосистеме Java. Стек: **JUnit 5** (запуск и assertions) + **REST Assured** (HTTP API). Подходит для enterprise-команд, Android-автоматизации (Espresso) и Selenium на Java.

---

## Зачем Java тестировщику

Java часто встречается в:

- бэкенде (Spring) и микросервисах;
- Android UI-тестах (Espresso);
- корпоративной автоматизации (Selenium + TestNG/JUnit);
- API-тестах через REST Assured.

Если вы уже знаете [тестирование API](./2.md) и [интеграционные тесты](./121.md), этот практикум закрепляет их на втором языке.

---

## Подготовка окружения

Нужны **JDK 17+** (LTS) и **Maven** или **Gradle**. Проверка:

```bash
java -version
mvn -version
```

Создайте Maven-проект или добавьте в `pom.xml` зависимости:

```xml
<dependencies>
  <dependency>
    <groupId>org.junit.jupiter</groupId>
    <artifactId>junit-jupiter</artifactId>
    <version>5.10.2</version>
    <scope>test</scope>
  </dependency>
  <dependency>
    <groupId>io.rest-assured</groupId>
    <artifactId>rest-assured</artifactId>
    <version>5.4.0</version>
    <scope>test</scope>
  </dependency>
</dependencies>
```

Структура:

```text
src/
├── main/java/          # код приложения (может быть пустым в учебном проекте)
└── test/java/
    └── api/
        └── HealthApiTest.java
```

Подробнее про JUnit в [каталоге инструментов](./118.md#junit-java).

---

## Шаг 1 — простой JUnit-тест (unit)

Проверим чистую функцию без сети — так же, как `test_calculator.py` в [1011](./1011.md):

```java
package utils;

public class StringUtils {
    public static boolean isBlank(String s) {
        return s == null || s.trim().isEmpty();
    }
}
```

```java
package utils;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.DisplayName;
import static org.junit.jupiter.api.Assertions.*;

class StringUtilsTest {

    @Test
    @DisplayName("Пустая строка распознаётся как blank")
    void blankStringIsBlank() {
        assertTrue(StringUtils.isBlank(""));
        assertTrue(StringUtils.isBlank("   "));
    }

    @Test
    @DisplayName("Непустая строка не blank")
    void nonBlankString() {
        assertFalse(StringUtils.isBlank("qa"));
    }
}
```

Запуск:

```bash
mvn test
```

---

## Шаг 2 — API-тест с REST Assured

Проверим публичный учебный API [JSONPlaceholder](https://jsonplaceholder.typicode.com):

```java
package api;

import io.restassured.RestAssured;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

import static io.restassured.RestAssured.*;
import static org.hamcrest.Matchers.*;

class PostsApiTest {

    @BeforeAll
    static void setup() {
        RestAssured.baseURI = "https://jsonplaceholder.typicode.com";
    }

    @Test
    void getPostReturns200AndTitle() {
        given()
            .pathParam("id", 1)
        .when()
            .get("/posts/{id}")
        .then()
            .statusCode(200)
            .body("id", equalTo(1))
            .body("title", not(emptyString()))
            .body("userId", greaterThan(0));
    }

    @Test
    void createPostReturns201() {
        String body = """
            {"title": "QA test", "body": "from REST Assured", "userId": 1}
            """;

        given()
            .header("Content-Type", "application/json")
            .body(body)
        .when()
            .post("/posts")
        .then()
            .statusCode(201)
            .body("title", equalTo("QA test"));
    }
}
```

Здесь **Given–When–Then** — аналог Arrange–Act–Assert из [автоматизации](./115.md).

---

## Шаг 3 — параметризация

Связь с [тест-дизайном](./127.md) — одна логика, несколько входов:

```java
import org.junit.jupiter.params.ParameterizedTest;
import org.junit.jupiter.params.provider.CsvSource;

import static org.junit.jupiter.api.Assertions.*;

class BoundaryTest {

    @ParameterizedTest
    @CsvSource({
        "0, false",
        "1, true",
        "100, true",
        "-1, false"
    })
    void orderQuantityValidation(int qty, boolean valid) {
        assertEquals(valid, qty > 0 && qty <= 100);
    }
}
```

---

## Связь с Selenium и Appium

Тот же Maven-проект можно расширить:

- **Selenium WebDriver** — см. [1181](./1181.md);
- **Appium Java Client** — см. [мобильное тестирование](./124.md).

Page Object и явные ожидания (`WebDriverWait`) — те же принципы, что в [1013](./1013.md).

---

## Типичные ошибки

| Ошибка | Решение |
|--------|---------|
| `Connection refused` | Проверьте URL, VPN, доступ к стенду |
| Тест зависит от порядка запуска | Изолируйте данные, не шарьте state между тестами |
| Хардкод токена в коде | Переменные окружения или `@BeforeAll` из CI secrets |
| Дублирование HTTP-логики | Вынесите `given()` в helper или базовый класс |

---

## Что дальше

1. [Интеграционное тестирование](./121.md) — стыки сервисов и БД.
2. [Selenium на Java](./1181.md) — UI-автоматизация.
3. [Git для QA](./1274.md#git--минимум-для-тестировщика) — хранение тестов в репозитории.
4. Запуск `mvn test` в [CI/CD](/encyclopedia/8-infra-security/8-04-devops-ci-cd/intro).

---

## Навигация по разделу «Тестирование»

- **Практикум:** [1011 Python](./1011.md) · [1012](./1012.md) · [1013](./1013.md) · [1014](./1014.md) · [1015 Java](./1015.md) · [1016 JS](./1016.md)
- **Бонус-модули:** [1274](./1274.md)
- **Маршрут:** [О разделе](./intro.md)

---
