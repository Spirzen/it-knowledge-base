---
tags: [engineer, developer, architector, required, beginner]
title: 'Паттерн "Прототип" (Prototype) в Java — копирование вместо создания с нуля'
description: >-
  Prototype в Java — clone, deep copy и copy constructor на примере отчета с
  дорогой инициализацией.
slug: /encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/134
sidebar_label: Prototype в Java
related:
  - title: Порождающие паттерны
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/111
  - title: Builder в Java
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/130
  - title: Современный синтаксис Java
    doc: encyclopedia/5-languages/5-03-java/300
---

import SchemaMakerPlay from '@site/src/components/SchemaMakerPlay.jsx';

# Паттерн "Прототип" (Prototype) в Java — копирование вместо создания с нуля

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

Обзор паттерна — в [порождающих паттернах](./111.md). Здесь — практический Prototype на Java для объектов с дорогой инициализацией.

<SchemaMakerPlay defaultDocName="Prototype" title="Эскиз Prototype" subtitle="Прототип и клоны через общий контракт" />

---

## Задача паттерна

**Prototype** позволяет создавать новый объект копированием существующего, без повторной полной инициализации.

Подходит, когда:

- конструктор тяжелый (загрузка шаблонов, конфигов, внешних ресурсов);
- объекты отличаются только состоянием;
- нужно быстро тиражировать заготовки.

---

## Пример — прототип отчета

```java
import java.util.HashMap;
import java.util.Map;

abstract class Report implements Cloneable {
    protected String template;
    protected DatabaseConnection dbConnection;
    protected Map<String, Object> settings;

    protected Report() {
        this.dbConnection = new DatabaseConnection();
        this.settings = loadDefaultSettings();
    }

    protected Map<String, Object> loadDefaultSettings() {
        Map<String, Object> defaults = new HashMap<>();
        defaults.put("period", "Q1");
        defaults.put("format", "pdf");
        return defaults;
    }

    @Override
    public Report clone() {
        try {
            Report cloned = (Report) super.clone();
            cloned.settings = new HashMap<>(this.settings);
            return cloned;
        } catch (CloneNotSupportedException e) {
            throw new IllegalStateException(e);
        }
    }

    abstract void generate();
}

class SalesReport extends Report {
    @Override
    void generate() {
        System.out.println("Генерация отчета по продажам");
    }
}

final class DatabaseConnection {
}
```

```java
Report prototype = new SalesReport();

Report report1 = prototype.clone();
Report report2 = prototype.clone();
report2.settings.put("period", "Q2");
```

---

## Shallow copy и Deep copy

| Тип копии | Что копируется |
|-----------|----------------|
| `Shallow` | Примитивы и ссылки (вложенные объекты общие) |
| `Deep` | Новые экземпляры вложенных изменяемых объектов |

В примере выше `settings` копируется глубоко относительно `Map`, чтобы изменения в одном отчете не влияли на другие.

---

## Альтернативы `clone()`

- copy constructor: `new Report(existing)`
- статический метод: `Report.copy(existing)`
- сериализация (реже, обычно дороже по CPU)

Во многих командах Java предпочитают явные copy-constructor методы, потому что `Cloneable` и `clone()` исторически считаются не самым удобным API.

---

## См. также

- [Порождающие паттерны — Прототип](./111.md#прототип)
- [Builder в Java](./130.md)
- [Factory Method в Java](./136.md)
