---
tags: [engineer, developer, architector, required, beginner]
title: 'Паттерн "Итератор" в Java — Iterable, пагинация и for-each'
description: >-
  Iterator в Java — Iterable, Iterator, PaginatedIterator для БД,
  ConcurrentModificationException и связь с Stream API.
slug: /encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/123
sidebar_label: Итератор в Java
related:
  - title: Поведенческие паттерны
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/113
  - title: Паттерн "Итератор" в C#
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/118
  - title: Коллекции Java
    doc: encyclopedia/5-languages/5-03-java/24
---

import SchemaMakerPlay from '@site/src/components/SchemaMakerPlay.jsx';

# Паттерн "Итератор" в Java — Iterable, пагинация и for-each

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>


<div class="callout callout--info">
  <div class="callout-title">Теория данных (раздел 3)</div>
  <div class="callout-body">
  Модель и масштаб — [Основы БД](/encyclopedia/3-data-markup/3-05-osnovy-baz-dannyh/intro), [опорные темы](/encyclopedia/3-data-markup/3-05-osnovy-baz-dannyh/12), [проектирование БД](design/116.md), [пакетная работа](/encyclopedia/3-data-markup/3-11-analiz-dannyh/433). Карта — [о разделе](./intro).
  </div>
</div>

Обзор GoF — в [поведенческих паттернах](./113.md). Аналог на C# (`yield`, LINQ) — в [Итератор в C#](./118.md). Здесь — **Java**: `Iterable` / `Iterator`, enhanced for и итератор, который скрывает пагинацию из БД.

<SchemaMakerPlay defaultDocName="Iterator" title="Эскиз Iterator" subtitle="hasNext, next и скрытая структура коллекции" />

---

## Задача паттерна

**Iterator** даёт последовательный обход элементов **без раскрытия** внутреннего устройства коллекции (массив, дерево, курсор в БД). Клиент работает через "окно" `hasNext()` / `next()`, не зная, что за ним.

В Java контракт зафиксирован в JDK:

| Тип | Роль |
|-----|------|
| `Iterable<T>` | Коллекция умеет выдать итератор (`iterator()`) |
| `Iterator<T>` | `hasNext()`, `next()`, `remove()` (опционально) |
| `for (T x : coll)` | Синтаксический сахар над `Iterable` |

---

## Пагинация из БД — свой Iterator

Клиент обходит пользователей как обычную последовательность; подгрузка страниц спрятана внутри итератора.

```java
import java.util.Iterator;
import java.util.List;
import java.util.NoSuchElementException;
import java.util.function.Function;

public class PaginatedIterator<T> implements Iterator<T> {
    private final Function<Integer, List<T>> pageFetcher;
    private final int pageSize;
    private List<T> currentPage;
    private int pageIndex = 0;
    private int itemIndex = 0;

    public PaginatedIterator(Function<Integer, List<T>> pageFetcher, int pageSize) {
        this.pageFetcher = pageFetcher;
        this.pageSize = pageSize;
        this.currentPage = pageFetcher.apply(0);
    }

    @Override
    public boolean hasNext() {
        if (itemIndex < currentPage.size()) {
            return true;
        }
        if (currentPage.size() < pageSize) {
            return false;
        }
        pageIndex++;
        currentPage = pageFetcher.apply(pageIndex);
        itemIndex = 0;
        return !currentPage.isEmpty();
    }

    @Override
    public T next() {
        if (!hasNext()) {
            throw new NoSuchElementException();
        }
        return currentPage.get(itemIndex++);
    }
}
```

Использование (Spring Data в лямбде — пример):

```java
Iterator<User> users = new PaginatedIterator<>(
    page -> userRepository.findAll(PageRequest.of(page, 50)).getContent(),
    50
);

while (users.hasNext()) {
    process(users.next());
}
```

Тот же сценарий можно выразить через `Stream` и `Spliterator`, но явный `Iterator` нагляден и совместим с любым кодом, ожидающим `Iterable`.

---

## Когда применять

| Ситуация | Зачем Iterator |
|----------|----------------|
| Разные структуры, один способ обхода | Единый контракт для списка, дерева, БД |
| Сложный обход (пагинация, ленивая подгрузка) | Детали не утекают в клиент |
| Несколько независимых проходов | У каждого вызова `iterator()` своё состояние |

---

## Риски

Для простого `ArrayList` отдельный класс итератора избыточен — достаточно встроенного API и `for-each`.

При изменении коллекции во время обхода (кроме `Iterator.remove()`) JVM бросает **`ConcurrentModificationException`** — fail-fast защита от "битого" состояния.

<div class="callout callout--warning">
  <div class="callout-title">Iterator и Stream</div>

  <div class="callout-body">
  Для одноразового ленивого пайплайна чаще уместен `Stream` ([Stream API](/encyclopedia/5-languages/5-03-java/295)).

  Собственный `Iterator` оправдан, когда нужна совместимость с legacy-кодом, точный контроль страниц или обход вне стандартных коллекций.
</div>
  </div>

---

## См. также

- [Поведенческие паттерны — Итератор](./113.md#паттерн-итератор)
- [Итератор в C#](./118.md)
- [Коллекции Java](/encyclopedia/5-languages/5-03-java/24)
- cursor/keyset в REST API — [шесть схем пагинации](/encyclopedia/2-system-network/2-09-osnovy-integratsionnogo-vzaimodeystviya/131)
