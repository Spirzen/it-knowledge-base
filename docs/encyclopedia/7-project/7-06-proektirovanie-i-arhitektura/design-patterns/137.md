---
tags: [engineer, developer, architector, required, beginner]
title: 'Паттерн "Адаптер" (Adapter) в Java — совместимость с легаси и внешними SDK'
description: >-
  Adapter в Java — переходник между несовместимыми интерфейсами, интеграция
  сторонней библиотеки без переписывания клиентского кода.
slug: /encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/137
sidebar_label: Adapter в Java
related:
  - title: Структурные паттерны
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/112
  - title: Abstract Factory в Java
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/135
  - title: Фасад в Java
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/129
---

import SchemaMakerPlay from '@site/src/components/SchemaMakerPlay.jsx';

# Паттерн "Адаптер" (Adapter) в Java — совместимость с легаси и внешними SDK

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

Обзор — в [структурных паттернах](./112.md). Здесь — практическая адаптация несовместимого интерфейса под контракт приложения.

<SchemaMakerPlay defaultDocName="Adapter" title="Эскиз Adapter" subtitle="Target, Adaptee и класс-переходник" />

---

## Задача паттерна

**Adapter** позволяет использовать существующий класс, чей интерфейс не совпадает с ожидаемым в системе.

Частый кейс — сторонний SDK или легаси-компонент, который менять нельзя.

---

## Пример

```java
interface NotificationService {
    void send(String userId, String text);
}

class LegacySmsGateway {
    void sendSms(String phone, String body) {
        System.out.println("Legacy SMS -> " + phone + ": " + body);
    }
}

class SmsGatewayAdapter implements NotificationService {
    private final LegacySmsGateway gateway;
    private final UserPhoneResolver resolver;

    SmsGatewayAdapter(LegacySmsGateway gateway, UserPhoneResolver resolver) {
        this.gateway = gateway;
        this.resolver = resolver;
    }

    @Override
    public void send(String userId, String text) {
        String phone = resolver.resolve(userId);
        gateway.sendSms(phone, text);
    }
}

interface UserPhoneResolver {
    String resolve(String userId);
}
```

Клиентский код работает только с `NotificationService`, не зная о деталях `LegacySmsGateway`.

---

## Когда использовать

- интеграция внешней библиотеки с неудобным API;
- миграция легаси в новую архитектуру;
- необходимость изолировать внешний контракт в одном месте.

---

## Adapter и Facade

| | Adapter | Facade |
|---|--------|--------|
| Цель | Преобразовать интерфейс | Упростить доступ к подсистеме |
| Объект | Часто один конкретный компонент | Группа компонентов |

---

## См. также

- [Структурные паттерны — Адаптер](./112.md#адаптер-adapter)
- [Abstract Factory в Java](./135.md)
- [Фасад в Java](./129.md)
