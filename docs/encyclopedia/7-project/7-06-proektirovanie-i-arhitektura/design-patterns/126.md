---
tags: [engineer, developer, architector, required, beginner]
title: 'Паттерн "Команда" в Java — undo в редакторе и очередь действий'
description: >-
  Command в Java — execute/undo, TextEditor, CommandHistory, сравнение с Memento
  и связь со Swing Action и CQRS.
slug: /encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/126
sidebar_label: Команда в Java
related:
  - title: Поведенческие паттерны
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/113
  - title: Паттерн "Команда" в C#
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/120
  - title: Паттерн "Мементо" в Java
    doc: encyclopedia/7-project/7-06-proektirovanie-i-arhitektura/design-patterns/125
---

import SchemaMakerPlay from '@site/src/components/SchemaMakerPlay.jsx';

# Паттерн "Команда" в Java — undo в редакторе и очередь действий

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Архитектору</span>

Обзор — в [поведенческих паттернах](./113.md). Практика на C# (MediatR, `ICommand`) — в [Команда в C#](./120.md). Здесь — классическая **Command** на Java с undo/redo.

<SchemaMakerPlay defaultDocName="Command" title="Эскиз Command" subtitle="Invoker, Command, Receiver" />

---

## Задача паттерна

**Command** превращает запрос в **объект** с методами `execute()` и при необходимости `undo()`. Такой объект можно передать, поставить в очередь, залогировать или отменить.

---

## Пример — текстовый редактор

```java
import java.util.ArrayDeque;
import java.util.Deque;

interface Command {
    void execute();
    void undo();
}

class TextEditor {
    private final StringBuilder text = new StringBuilder();

    void insert(int pos, String str) {
        text.insert(pos, str);
    }

    void delete(int pos, int length) {
        text.delete(pos, pos + length);
    }

    String getText() {
        return text.toString();
    }
}

class InsertCommand implements Command {
    private final TextEditor editor;
    private final int position;
    private final String text;

    InsertCommand(TextEditor editor, int position, String text) {
        this.editor = editor;
        this.position = position;
        this.text = text;
    }

    @Override
    public void execute() {
        editor.insert(position, text);
    }

    @Override
    public void undo() {
        editor.delete(position, text.length());
    }
}

class CommandHistory {
    private final Deque<Command> history = new ArrayDeque<>();

    void execute(Command cmd) {
        cmd.execute();
        history.push(cmd);
    }

    void undo() {
        if (!history.isEmpty()) {
            history.pop().undo();
        }
    }
}
```

```java
TextEditor editor = new TextEditor();
CommandHistory history = new CommandHistory();

history.execute(new InsertCommand(editor, 0, "Hello"));
history.execute(new InsertCommand(editor, 5, " World"));
System.out.println(editor.getText()); // Hello World

history.undo();
System.out.println(editor.getText()); // Hello
```

---

## Command в Java-экосистеме

| Место | Как проявляется Command |
|-------|-------------------------|
| **Swing `Action`** | Объект действия с `actionPerformed` |
| **JUnit / TestNG** | Иногда оборачивают шаги в команды |
| **CQRS** | `CreateOrderCommand` как DTO намерения (см. [интеграционные паттерны](./115.md)) |

В Spring Boot для HTTP-команд чаще используют сервисные методы или MediatR-подобные библиотеки; учебная схема `Command` + `Handler` на C# — в [120](./120.md).

---

## Когда применять и риски

| Ситуация | Command |
|----------|---------|
| Undo/redo | Каждое действие — класс с обратимой логикой |
| Очередь, отложенное выполнение | Команда сериализуется и уходит в worker |
| Макросы | Список команд выполняется подряд |

**Минус** — много мелких классов. Для одного вызова сервиса достаточно метода без паттерна.

Откат через полный снимок состояния — [Memento](./125.md).

---

## См. также

- [Поведенческие паттерны — Команда](./113.md#паттерн-команда)
- [Команда в C#](./120.md)
- [Мементо в Java](./125.md)
