---
title: 5.10. Ruby
---

# 5.10. Ruby

# Раздел в разработке

## Основы языка

```
Особенности языка
Синтаксис
Сфера применения

```

Документацию Ruby можно получить здесь https://www.ruby-lang.org/ru/

Чит-лист https://cheatsheets.zip/ruby

Какие возможности предоставляет Ruby?
	- *описывать логику так, чтобы она была понятна человеку, а не только машине*;
	- *написание скриптов, которые упрощают повседневную работу: обработка файлов, миграция данных, тестирование, CI/CD*;
	- *создавать полноценные backend-сервисы, REST API, микросервисы, серверные приложения*;
	- *строить системы хранения и извлечения информации*;
	- *взаимодействовать с сетью, API, файловой системой, GUI, IoT, облачными сервисами*;
	- *использование для парсинга, анализа, преобразования данных, работы с JSON, XML, YAML*;
	- *изменять поведение классов и объектов во время выполнения*;
	- *построение настольных приложений с графическим интерфейсом*;
	- *использование в устройствах с ограниченными ресурсами, где важна простота кода*;
	- *реализация шифрования, проверки целостности, безопасности данных*.




## История Ruby

```
История Ruby
```

## Знаки препинания

Два важных вопроса, которые мучают начинающих программистов:
1. Когда использовать кавычки двойные (`"`), одинарные (`'`), а когда апострофы (`’`)?
2. Когда использовать точки (`.`), запятые (`,`) и точку с запятой (`;`)?

Двойные (") — интерполируемые строки:
```
name = "Alice"
puts "Hello, #{name}"
```
Одинарные (`'`) — без интерполяции:
```
puts 'Hello, #{name}' # выводит как есть
```
Апострофы (`’`) — не поддерживаются.

Точка (`.`) используется для вызова методов:
```
"hello".upcase
```
Запятая (`,`) для разделения параметров и элементов:
```
arr = [1, 2, 3]
def greet(name, age); end
```
Точка с запятой используется для нескольких выражений в одной строке:
```
x = 5; y = 10; puts x + y
```
Но это не рекомендуется.

Нижние подчеркивания бывают как частью стиля, так и частью синтаксиса:

`_name` - соглашение для неиспользуемых переменных:
```
first, _, third = [1, 2, 3]
```
Приватные методы в Ruby выглядят как `private def logger`, а не `_logger`.

`_` может использоваться как игнорирование:
```
[1,2,3].each { |_, value| puts value }
```
а также `_` используется как разделитель в числах - `1_000_000`.

В Ruby символ «`|`» означает побитовое ИЛИ:
```
5 | 3  # => 7
```
Также `|` перегружается в классах, например, в Set, Array - объединение.

`||` же является логическим ИЛИ. Важно отметить, что в Ruby, как и в JS, логическое ИЛИ с сокращённым вычислением (`||`) используется так, что выбирается именно первое истинное значение, а не только true/false, к примеру 0 не будет истиной.


## Типы данных

```
Типы данных и переменные
```

## Операторы и циклы

```
Условные операторы и циклы
```

## Фреймворки

```
Фреймворки
```

## Работа с БД

```
Работа с данными
ActiveRecord - ORM, входящий в состав фреймворка Ruby on Rails. Простота использования, тесная интеграция с Rails. Применяется в веб-приложениях на Ruby on Rails.

```

## Асинхронность

```
Асинхронность
```

## Важные классы и интерфейсы

```
Важные классы и интерфейсы, методы Ruby

Ruby Numeric, Regexp, ARGF, Bignum, IO, Exception, Method, Enumerator, Process, Module, Random, Symbol, Range, Thread, Rational, Proc, Fiber, Float, Kernel, Class, Complex, BasicObject, Time, Object, Hash, String, Math, Integer, Array, File

Что и когда применять (библиотеки часто распространенные, классы и методы) при разработке в Ruby, примеры основных часто распространенных приложений или задач
```

## Популярные проекты на Ruby

```
Примеры интересных архитектур Ruby
Популярные проекты на Ruby

```


## Первая программа на Ruby

```
Первая программа на Ruby
```