---
tags: [all, analytic, engineer, developer, architector, tester, required, beginner]
title: Оператор SELECT — синтаксис и стиль
description: Логический порядок выполнения SELECT, проекция, WHERE, DISTINCT, ORDER BY и правила читаемого форматирования запросов.
---

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


import ExternalPlayEmbed from '@site/src/components/ExternalPlayEmbed';


# Оператор SELECT — синтаксис и стиль

`SELECT` — главный способ **прочитать** данные: что показать, откуда, какие строки оставить (`WHERE`), в каком порядке (`ORDER BY`). JOIN, группировка и подзапросы почти всегда строятся вокруг этого каркаса.

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Аналитику</span>
<span class="complexity-badge">Тестировщику</span>
<span class="complexity-badge">Архитектору</span>

**Перед чтением:** [Операторы](/encyclopedia/4-code-dev/4-02-chto-takoe-kod-i-kak-on-rabotaet/33) — общие понятия оператора, операнда, приоритетов и типов операций; здесь те же идеи в синтаксисе запросов.

---

## Логическая структура SELECT

Текстовый порядок предложений в запросе **не совпадает** с порядком выполнения СУБД:

```sql
SELECT   /* проекция: столбцы и выражения */
FROM     /* источник: таблица, представление, подзапрос */
WHERE    /* фильтрация строк до группировки */
GROUP BY /* группировка — при наличии агрегатов */
HAVING   /* фильтрация групп */
ORDER BY /* сортировка результата */
LIMIT    /* ограничение числа строк — PostgreSQL */
```

**Логический порядок выполнения** для простого запроса без группировки:

1. `FROM` — формирование исходного множества строк;
2. `WHERE` — отбор по предикату;
3. `SELECT` — вычисление выражений и проекция столбцов;
4. `ORDER BY` — сортировка;
5. `LIMIT` / `OFFSET` — обрезка результата.

Псевдоним столбца из `SELECT` **нельзя** использовать в `WHERE` того же уровня — `WHERE` выполняется раньше проекции.

Подробная схема с `JOIN`, `HAVING`, оконными функциями: [Принципы работы SQL-движка](./2.md#как-работает-sql).

---

<span id="краткая-шпаргалка-конструкции-чтения"></span>

## Краткая шпаргалка — конструкции чтения

Основные ключевые слова в `SELECT`-запросе и их роль (как в учебных шпаргалках по SQL):

| Конструкция | Назначение |
| :--- | :--- |
| **SELECT** | Какие столбцы и выражения вернуть клиенту |
| **FROM** | Откуда брать строки (таблица, представление, подзапрос) |
| **WHERE** | Отфильтровать строки **до** группировки |
| **AS** | Псевдоним столбца или таблицы в этом запросе |
| **JOIN** | Собрать строки из двух и более таблиц по условию связи |
| **AND** / **OR** | Несколько условий: должны выполниться все / хотя бы одно |
| **IN** | Значение входит в заданный список |
| **LIKE** | Поиск по шаблону (`%`, `_`) |
| **IS NULL** | Строки, где значение отсутствует |
| **CASE** | Условное значение в выражении (аналог `if` в проекции) |
| **GROUP BY** | Сгруппировать строки для агрегатов |
| **HAVING** | Отфильтровать **группы** (как `WHERE`, но после `GROUP BY`) |
| **ORDER BY** | Порядок строк в ответе; **DESC** — по убыванию |
| **LIMIT** | Сколько строк вернуть (в SQL Server — **TOP**, в стандарте — **FETCH**) |

`SELECT` в тексте запроса пишут первым, но СУБД обрабатывает блоки в [логическом порядке](./2.md#как-работает-sql). Агрегаты (`COUNT`, `SUM`, …) — в [Фильтрации и группировке](./6.md); типы **JOIN** — в [55](./55.md#chetyre-osnovnyh-join-na-odnom-primere).

<ExternalPlayEmbed example="lab/sql-trainer" title="SQL-тренажёр" minHeight={480} />

---

## Проекция данных

```sql
-- Явный список столбцов (рекомендуется в прикладном коде)
SELECT product_id, name, price FROM products;

-- Все столбцы — только для отладки
SELECT * FROM products;

-- Вычисляемый столбец и псевдоним
SELECT name, price, price * 1.2 AS price_with_vat FROM products;

-- Уникальные значения столбца
SELECT DISTINCT category FROM products;
```

---

## Фильтрация WHERE

| Конструкция | Назначение |
| :--- | :--- |
| `=`, `<>`, `<`, `<=`, `>`, `>=` | Сравнение |
| `IS NULL`, `IS NOT NULL` | Проверка отсутствия значения |
| `BETWEEN a AND b` | Диапазон включительно |
| `IN (...)` | Принадлежность списку |
| `LIKE 'шаблон'` | `%` — любая длина, `_` — один символ |
| `ILIKE` | Регистронезависимый поиск (PostgreSQL) |
| `AND`, `OR`, `NOT` | Приоритет: `NOT` > `AND` > `OR`; скобки обязательны |

**Трёхзначная логика:** сравнение с `NULL` через `=` или `<>` даёт `UNKNOWN`, а не `TRUE`/`FALSE`. Условие `WHERE price = NULL` не вернёт строк — используйте **IS NULL** / **IS NOT NULL**.



<ExternalCodeEmbed example="sql/sql-107-001" title="Фильтрация WHERE" minHeight={354} />



---

## Сортировка ORDER BY

```sql
SELECT name, price FROM products ORDER BY price DESC;

SELECT category, name, price
FROM products
ORDER BY category ASC, price DESC;

-- PostgreSQL: явная позиция NULL
SELECT name, stock_qty
FROM products
ORDER BY stock_qty NULLS LAST;
```

Сортировка по номеру столбца (`ORDER BY 2`) допустима, но снижает читаемость — предпочтительны имена.

---

## Правила форматирования

- Каждое предложение `SELECT`, `FROM`, `WHERE`, `ORDER BY` — с новой строки.
- Список из более двух столбцов — по одному на строку.
- Бинарные операторы окружают пробелами: `price = 100`, не `price=100`.
- Сложные условия `WHERE` выравнивают по `AND` / `OR`.
- Нетривиальные условия поясняют комментарием `--`.

Пример оформления:



<ExternalCodeEmbed example="sql/sql-107-002" title="Правила форматирования" minHeight={318} />



---

## Тестовые данные для практики



<ExternalCodeEmbed example="sql/sql-107-003" title="Тестовые данные для практики" minHeight={354} />



Готовые запросы на похожей схеме магазина (фильтр по цене, даты, сортировка) с разбором каждой строки — [SQL — реальные кейсы](/lab/Примеры/1152). Полный DDL и заказы — [практикум shop_data](./111.md).

---

## Контрольные вопросы

1. Почему `WHERE price = NULL` не возвращает строк?
2. В каком порядке СУБД выполняет `FROM`, `WHERE` и `SELECT`?
3. Чем `SELECT *` отличается от явного списка столбцов при изменении схемы таблицы?
4. Почему `LIKE 'ABC%'` может использовать индекс, а `LIKE '%ABC'` — обычно нет?

---

## См. также

- [Фильтрация и группировка в SQL](./6.md)
- [Алиасы, JOIN и объединение таблиц](./55.md)
- [Чтение и анализ сложных SQL-запросов](./21.md)
- [SQL — реальные кейсы](/lab/Примеры/1152) — `SELECT`, `WHERE`, `ORDER BY` на схеме магазина с разбором по строкам

---
