---
tags: [all, analytic, engineer, developer, architector, tester, required, beginner]
title: Практикум shop_data
description: Учебная схема интернет-магазина для PostgreSQL — DDL и примеры запросов по темам курса SQL.
---

import ExternalCodeEmbed from '@site/src/components/ExternalCodeEmbed';


# Практикум shop_data

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>
<span class="complexity-badge">Аналитику</span>
<span class="complexity-badge">Архитектору</span>

Схема **shop_data** — единая учебная база для практики в PostgreSQL (DBeaver, psql). Запросы ниже соответствуют планам занятий курса и папке Examples. В браузерном [SQL-тренажёре](./1.md) доступна та же схема **shop_data** плюс `users` и `cities` для JOIN. На сервере PostgreSQL выполняйте те же запросы в DBeaver/psql.

Для тех же таблиц с **построчным разбором**, ожидаемыми цифрами и заданиями "попробуйте" (удобно для лабораторной и поиска в Google) — [SQL — реальные кейсы](/lab/Примеры/1152).

---

## Подготовка схемы



<ExternalCodeEmbed example="sql/sql-111-001" title="Подготовка схемы" minHeight={720} />



Проверка подключения:

```sql
SELECT version();
SELECT current_database(), current_user;
SELECT pg_size_pretty(pg_database_size(current_database()));
```

---

## SELECT и фильтрация

```sql
SELECT product_id, name, price
FROM shop_data.products
WHERE category = 'Книги' AND price BETWEEN 500 AND 2000;

SELECT DISTINCT category FROM shop_data.products;

SELECT name, price
FROM shop_data.products
WHERE name ILIKE '%кабель%'
ORDER BY price DESC NULLS LAST;
```

---

## Агрегаты и GROUP BY

```sql
SELECT
    category,
    COUNT(*) AS cnt,
    AVG(price) AS avg_price,
    MAX(price) AS max_price
FROM shop_data.products
GROUP BY category
HAVING COUNT(*) >= 1;
```

---

## JOIN

```sql
SELECT
    o.order_id,
    c.full_name,
    o.order_date,
    oi.quantity,
    p.name AS product_name
FROM shop_data.orders o
JOIN shop_data.customers c ON c.customer_id = o.customer_id
JOIN shop_data.order_items oi ON oi.order_id = o.order_id
JOIN shop_data.products p ON p.product_id = oi.product_id
ORDER BY o.order_date DESC;
```

---

## Подзапросы и EXISTS

```sql
SELECT c.full_name
FROM shop_data.customers c
WHERE EXISTS (
    SELECT 1 FROM shop_data.orders o WHERE o.customer_id = c.customer_id
);

SELECT name, price
FROM shop_data.products p
WHERE price > (
    SELECT AVG(price) FROM shop_data.products p2 WHERE p2.category = p.category
);
```

---

## Оконные функции

> Полный разбор — [Встроенные и оконные функции в SQL](./7.md#оконные-функции) (ранжирование, накопительные суммы, доли, LAG/LEAD, отчёты без Excel, кейсы).

> **Общая база:** именованный вызов с аргументами и возвратом в коде — [функции в коде](/encyclopedia/4-code-dev/4-02-chto-takoe-kod-i-kak-on-rabotaet/4). В SQL выражения `COUNT()`, `SUM()`, `OVER` — тот же приём на уровне запроса, другой синтаксис.


```sql
SELECT
    order_id,
    customer_id,
    order_date,
    ROW_NUMBER() OVER (PARTITION BY customer_id ORDER BY order_date) AS order_num,
    SUM(oi.quantity * oi.unit_price) OVER (
        PARTITION BY o.customer_id
        ORDER BY o.order_date
        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW
    ) AS cumulative_spent
FROM shop_data.orders o
JOIN shop_data.order_items oi ON oi.order_id = o.order_id;
```

---

## Системные каталоги

```sql
SELECT table_name
FROM information_schema.tables
WHERE table_schema = 'shop_data'
ORDER BY table_name;

SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_schema = 'shop_data' AND table_name = 'products'
ORDER BY ordinal_position;
```

Подробнее: [Словарь данных и системные каталоги](./105.md).

---

## План и статистика

```sql
EXPLAIN (ANALYZE, BUFFERS)
SELECT * FROM shop_data.orders WHERE customer_id = 1;

SELECT schemaname, relname, last_analyze, n_live_tup, n_mod_since_analyze
FROM pg_stat_user_tables
WHERE schemaname = 'shop_data';

ANALYZE shop_data.orders;
```

---

## Транзакции и блокировки

```sql
BEGIN;
UPDATE shop_data.orders SET status = 'processing' WHERE order_id = 1;
COMMIT;

BEGIN;
SELECT order_id FROM shop_data.orders
WHERE status = 'new'
FOR UPDATE SKIP LOCKED
LIMIT 1;
COMMIT;
```

Подробнее: [Блокировки и конкурентный доступ](./110.md#skip-locked-ochered) (очередь на одну задачу) и [пакетная обработка](./110.md#skip-locked-paket).

---

## Резервное копирование

```bash
pg_dump -Fc -f shop_data.dump -n shop_data your_database
pg_restore -l shop_data.dump
```

Подробнее: [Резервное копирование и восстановление](./106.md).

---

## Карта тем курса

| Урок | Тема | Статья в энциклопедии |
| :--- | :--- | :--- |
| 1–3 | Введение, модель | [Эволюция систем хранения данных](./102.md), [Реляционная модель данных](./103.md) |
| 4 | Нормализация | [Нормализация данных](./104.md) |
| 5–8 | Типы, DDL, ограничения | [Типы данных в SQL](./33.md), [DDL - определение структуры базы данных](./44.md), [Ограничения целостности в SQL](./444.md) |
| 9 | Каталоги | [Словарь данных и системные каталоги](./105.md) |
| 10–12 | SELECT, фильтр, сортировка | [Оператор SELECT — синтаксис и стиль](./107.md), [Фильтрация и трёхзначная логика](./109.md), [Фильтрация и группировка в SQL](./6.md) |
| 13–17 | Агрегаты, JOIN, CTE | [Фильтрация и группировка в SQL](./6.md), [Алиасы, JOIN и объединение таблиц](./55.md), [Общие табличные выражения (CTE)](./551.md) |
| 15 | Подзапросы | [Подзапросы, EXISTS и IN](./108.md) |
| 18 | CRUD | [CRUD-операции и язык манипуляции данными (DML)](./5.md) |
| 19 | Окна | [Встроенные и пользовательские функции в SQL](./7.md) |
| 20 | JSON, массивы | [Практикум PostgreSQL по JSONB](./66.md) |
| 21–24 | VIEW, транзакции, процедуры | [Представления (VIEW) - виртуальные таблицы](./8.md), [Транзакции, изоляция и блокировки](./77.md), [Хранимые процедуры и триггеры](./88.md) |
| 25–27 | Индексы | [Сложные индексы](./884.md), [Оптимизация SQL-запросов](./881.md) |
| 28–29 | EXPLAIN, ANALYZE | [Оптимизация SQL-запросов](./881.md) |
| 30–31 | Backup, права | [Резервное копирование и восстановление PostgreSQL](./106.md), [Взаимодействие приложений с СУБД через SQL](./4.md) |

Полная шпаргалка синтаксиса: [Шпаргалка с типичными задачами по SQL](./885.md), справочник: [Справочник по SQL](./883.md). Галерея кейсов с разбором для учебы: [SQL — реальные кейсы](/lab/Примеры/1152).

---
