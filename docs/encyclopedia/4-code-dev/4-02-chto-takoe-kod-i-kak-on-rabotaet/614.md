---
tags: [all, engineer, developer, architector, required, beginner]
title: "Однострочные приёмы в коде"
description: "Краткие идиоматичные записи на одной строке — обмен значениями, срезы, включения, слияние коллекций; когда уместны и куда смотреть по языкам."
---

# Однострочные приёмы в коде

<div class="article-tags">
  <span class="tag tag-required">ОБЯЗАТЕЛЬНО</span>
  <span class="tag tag-beginner">ДЛЯ НОВИЧКОВ</span>
</div>

<span class="complexity-badge">Разработчику</span>

См. также: [Функции и выражения](./4.md) · [Что такое код](./1.md) · [Python — однострочные приёмы](/encyclopedia/5-languages/5-02-python/38)

---

## Однострочные приёмы в коде

### Что такое однострочный приём?

> Однострочный приём, однострочник, или ванлайнер - это кусок кода, который выполняет сложную или многошаговую задачу всего **в одну строчку**.

Например, это обычный подход в три строки:

```python
temp = a
a = b
b = temp
```

А однострочный приём выполняется так:

```python
a, b = b, a
```

**Однострочник** (one-liner) в повседневной разработке — это не обязательно «вся программа в одной строке». Чаще речь о **короткой идиоматичной записи** типовой операции: обмен двух переменных, разворот строки, фильтр списка, слияние словарей. Язык высокого уровня позволяет выразить намерение компактно, если вы уже знаете базовые конструкции — [переменные](/encyclopedia/4-code-dev/4-03-vypolnenie-koda/115), [циклы](./5.md) и [функции](./4.md).

Отдельный смысл у **однострочных команд в shell** (`grep`, `awk`, `perl -e`) — это инструменты ОС и автоматизации, см. [терминал](/encyclopedia/2-system-network/2-05-terminal/intro), [Bash](/encyclopedia/5-languages/5-25-bash/123), [галерея однострочников и скриптов](/lab/Примеры/1151), [справочник RegEx](./615.md) и [готовые паттерны для grep](/lab/Примеры/615). Здесь — приёмы **внутри исходного кода** на языках общего назначения.

---

### Зачем они нужны

| Ситуация | Польза |
|----------|--------|
| REPL, ноутбук, скрипт на пять строк | Быстро проверить идею без лишнего шума |
| Обработка данных (логи, CSV, JSON) | Фильтр, flatten, подсчёт частот — одна понятная строка |
| Чтение чужого кода | Идиомы языка «узнаются» сразу, если вы их уже видели |
| Рефакторинг | Иногда длинный цикл сворачивается во [включение](./5.md) или цепочку методов |

Классический пример из Python: `a, b = b, a` меняет значения **без временной переменной** за счёт [распаковки кортежа](https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences); разворот строки — `s[::-1]` через [срез с шагом](/encyclopedia/5-languages/5-02-python/38#razvorot-stroki-i-palindrom).

---

### Когда лучше развернуть в несколько строк

<div class="callout callout--warning">
  <div class="callout-title">Читаемость важнее краткости</div>

  <div class="callout-body">
  Однострочник, который коллега не разберёт с первого взгляда, — кандидат в [рефакторинг](./612.md).

  В [Go](/encyclopedia/5-languages/5-10-go/17) намеренно нет тернарного оператора `?:`, чтобы не поощрять запутанные выражения.

  То же правило для production-кода на любом языке: сначала ясность, потом лаконичность.
</div>
</div>

Признаки, что строку стоит разбить:

- вложенные включения, тернарные операторы и вызовы в одном выражении;
- побочные эффекты (изменение глобального состояния, I/O) внутри «красивой» строки;
- бизнес-логика, которую тестируют по отдельным шагам.

[Методы рефакторинга](./612.md) и [запахи кода](/encyclopedia/7-project/7-12-konstruirovanie-po/intro) помогают удержать баланс.

---

<span id="sravnenie-yazykov"></span>

### Десять частых задач — Python и аналоги

Полный разбор с примерами и оговорками — в статье [Однострочные приёмы Python](/encyclopedia/5-languages/5-02-python/38). Ниже — **сводка** для сравнения языков (синтаксис может отличаться по версии и стилю команды).

| Задача | Python | JavaScript | Java | C# | Go |
|--------|--------|------------|------|-----|-----|
| Поменять `a` и `b` | `a, b = b, a` | `[a, b] = [b, a]` | через `temp` или XOR для чисел | `(a, b) = (b, a)` | `a, b = b, a` |
| Развернуть строку | `s[::-1]` | `[...s].reverse().join('')` | `new StringBuilder(s).reverse()` | `new string(s.Reverse())` | руны в `[]rune`, цикл |
| Палиндром | `s == s[::-1]` | `s === [...s].reverse().join('')` | `s.equals(new StringBuilder(s).reverse())` | сравнение с `Reverse()` | цикл или `utf8` |
| Факториал `n` | `math.prod(range(1, n+1))` | `[...Array(n)].reduce((p,_,i)=>p*(i+1),1)` | `IntStream.rangeClosed(1,n).reduce(1,(a,b)->a*b)` | `Enumerable.Range(1,n).Aggregate(1,(a,b)=>a*b)` | цикл `for` |
| Flatten списка списков | `[x for sub in lst for x in sub]` | `lst.flat()` | `lst.stream().flatMap(Collection::stream)` | `lst.SelectMany(x => x)` | вложенные `append` |
| Чётные из диапазона | `[x for x in range(10) if x%2==0]` | `[...Array(10).keys()].filter(x=>x%2===0)` | `IntStream.range(0,10).filter(i->i%2==0)` | `Enumerable.Range(0,10).Where(i=>i%2==0)` | цикл + `append` |
| Слить два словаря | `&#123;&#42;&#42;d1, &#42;&#42;d2&#125;` | `&#123;...d1, ...d2&#125;` | `Map` + `putAll` | LINQ / цикл по ключам | нет встроенного `map` до Go 1.21+ |
| Подсчёт элементов | `Counter(lst)` | `Map` + цикл или `reduce` | `Collectors.groupingBy` | `GroupBy` | `map[T]int` |
| Уникальные значения | `set(lst)` | `[...new Set(lst)]` | `stream().distinct()` | `lst.Distinct()` | `map` как множество |
| Список строк → одна | `' '.join(lst)` | `lst.join(' ')` | `String.join(" ", lst)` | `string.Join(" ", lst)` | `strings.Join(lst, " ")` |

<div class="callout callout--tip">
  <div class="callout-title">Куда углубиться по языкам</div>

  <div class="callout-body">
  <strong>Python</strong> — <a href="/encyclopedia/5-languages/5-02-python/38">глава про one-liners</a>, <a href="/encyclopedia/5-languages/5-02-python/19">типы и коллекции</a>, <a href="/encyclopedia/5-languages/5-02-python/24">функции</a>.<br/>
  <strong>JavaScript</strong> — <a href="/encyclopedia/5-languages/5-01-javascript/18">строки</a>, <a href="/encyclopedia/5-languages/5-01-javascript/122">справочник методов</a>.<br/>
  <strong>Java</strong> — <a href="/encyclopedia/5-languages/5-03-java/24">коллекции</a>, Stream API.<br/>
  <strong>C#</strong> — <a href="/encyclopedia/5-languages/5-05-csharp/intro">раздел C#</a>, LINQ.<br/>
  <strong>Go</strong> — явные циклы; краткость через <a href="/encyclopedia/5-languages/5-10-go/intro">идиомы Go</a>, не через «магические» однострочники.
</div>
</div>

---

### Связь с другими темами раздела

- **Синтаксический сахар** — сокращения, которые компилятор или интерпретатор разворачивает в более длинную форму ([уровни абстракции](/encyclopedia/4-code-dev/4-07-paradigmy-i-urovni-abstraktsii/1)).
- **Декларативный стиль** — вы описыете *что* нужно (чётные числа, уникальные ключи), платформа выбирает *как* (SQL, включения, LINQ).
- **Идиоматичный код** — принятый в сообществе языка способ писать типовые вещи; однострочники — его видимая часть.

После этой главы имеет смысл открыть [языки программирования](/encyclopedia/5-languages/intro) и выбрать один раздел для практики — не смешивая правила `set`/`dict`/`Stream` в одном учебном дне.
