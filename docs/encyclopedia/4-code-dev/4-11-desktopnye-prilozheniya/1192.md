---
tags: [reference, engineer, developer, architector, notrequired, advanced]
title: "Справочник по WPF — элементы UI"
description: "Рецепты WPF — Window, Label, Button, TextBox, привязки, списки, меню в XAML и C#."
referenceType: language
---

# Справочник по WPF — элементы UI

<div class="article-tags"><span class="tag tag-reference">СПРАВОЧНИК</span></div>

---

## Назначение

Пошаговые рецепты **WPF** — в первую очередь **XAML** + при необходимости code-behind. Практикум — [119.md](./119.md), платформа — [116.md](./116.md). Синтаксис XAML — [справочник XAML](/encyclopedia/3-data-markup/3-04-konfiguratsii-i-dannye/6). Общие имена элементов — [Архитектура десктопных приложений](/encyclopedia/4-code-dev/4-11-desktopnye-prilozheniya/1#элементы-управления-и-ввод-данных); UI-поток — [112.md](/encyclopedia/4-code-dev/4-11-desktopnye-prilozheniya/112.md). Язык C# — [раздел C#](/encyclopedia/5-languages/5-05-csharp/intro).

---

## Как пользоваться

1. Создайте проект WPF (`<UseWPF>true</UseWPF>`).
2. Разметку добавляйте в `MainWindow.xaml` внутри корневого контейнера (`Grid`, `StackPanel`).
3. Логику — в ViewModel с `{Binding}` или в `*.xaml.cs` для простых обработчиков.
4. Обновление UI из фона — `Dispatcher.Invoke` или `await` после `Task.Run`.

---

## Главное окно (Window)

**MainWindow.xaml:**

```xml
<Window x:Class="App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Моё приложение"
        Height="480" Width="640"
        MinWidth="400" MinHeight="300">
    <!-- контент ниже -->
</Window>
```

**App.xaml.cs** — `StartupUri="MainWindow.xaml"` или `OnStartup` с `new MainWindow().Show()`.

---

## Контейнер (Grid / StackPanel)

```xml
<Grid Margin="12">
  <Grid.RowDefinitions>
    <RowDefinition Height="Auto"/>
    <RowDefinition Height="*"/>
  </Grid.RowDefinitions>
</Grid>

<StackPanel Orientation="Vertical" Margin="12" Spacing="8">
  <!-- дочерние элементы -->
</StackPanel>

<DockPanel LastChildFill="True">
  <Menu DockPanel.Dock="Top"/>
  <StatusBar DockPanel.Dock="Bottom"/>
  <Grid><!-- центр --></Grid>
</DockPanel>
```

---

## Label (метка)

```xml
<Label Content="Имя пользователя:" VerticalAlignment="Center"/>
<TextBlock Text="Статический текст" TextWrapping="Wrap"/>
```

Code-behind:

```csharp
StatusLabel.Content = "Сохранено";
```

Привязка:

```xml
<Label Content="{Binding StatusText}"/>
```

---

## TextBox — однострочный

```xml
<TextBox x:Name="NameBox"
         Width="240"
         MaxLength="100"
         Text="по умолчанию"/>
```

```csharp
string value = NameBox.Text;
NameBox.Clear();
NameBox.Text = "новое";

// Enter
NameBox.KeyDown += (s, e) => { if (e.Key == Key.Enter) Submit(); };
```

Привязка (MVVM):

```xml
<TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
```

---

## TextBox — многострочный

```xml
<TextBox AcceptsReturn="True"
         TextWrapping="Wrap"
         VerticalScrollBarVisibility="Auto"
         Height="120"/>
```

---

## Поле пароля

```xml
<PasswordBox x:Name="PwdBox" Width="200"/>
```

```csharp
string pwd = PwdBox.Password;
```

---

## Button (кнопка)

```xml
<Button Content="Сохранить"
        Width="100"
        IsDefault="True"
        Click="Save_Click"/>
```

```csharp
private void Save_Click(object sender, RoutedEventArgs e) { /* ... */ }
```

Команда (MVVM):

```xml
<Button Content="Сохранить" Command="{Binding SaveCommand}"/>
```

---

## CheckBox (флажок)

```xml
<CheckBox Content="Уведомления" IsChecked="True"/>
```

```xml
<CheckBox Content="Согласен" IsChecked="{Binding IsAgreed}"/>
```

---

## RadioButton (переключатель)

```xml
<StackPanel>
  <RadioButton Content="Пользователь" GroupName="Role" IsChecked="True"/>
  <RadioButton Content="Админ" GroupName="Role"/>
</StackPanel>
```

---

## ComboBox (выпадающий список)

```xml
<ComboBox Width="180" SelectedIndex="0">
  <ComboBoxItem Content="Работа"/>
  <ComboBoxItem Content="Личное"/>
</ComboBox>
```

Привязка к коллекции:

```xml
<ComboBox ItemsSource="{Binding Categories}"
          SelectedItem="{Binding SelectedCategory}"
          DisplayMemberPath="Name"/>
```

---

## ListBox (список)

```xml
<ListBox ItemsSource="{Binding Items}"
         SelectedItem="{Binding CurrentItem}"
         Height="160"/>
```

Шаблон элемента — **DataTemplate** (см. [119.md](./119.md)).

---

## DataGrid (таблица)

```xml
<DataGrid ItemsSource="{Binding People}"
          AutoGenerateColumns="True"
          IsReadOnly="True"
          CanUserAddRows="False"/>
```

Явные колонки:

```xml
<DataGrid ItemsSource="{Binding People}" AutoGenerateColumns="False">
  <DataGrid.Columns>
    <DataGridTextColumn Header="Имя" Binding="{Binding Name}"/>
    <DataGridTextColumn Header="Возраст" Binding="{Binding Age}"/>
  </DataGrid.Columns>
</DataGrid>
```

---

## ProgressBar (индикатор прогресса)

```xml
<ProgressBar Minimum="0" Maximum="100" Value="50" Height="20"/>
<ProgressBar IsIndeterminate="True" Height="20"/>
```

```xml
<ProgressBar Value="{Binding ProgressPercent}"/>
```

---

## Slider (ползунок)

```xml
<Slider Minimum="0" Maximum="100" Value="{Binding Volume}" TickFrequency="10"/>
```

---

## Поле поиска (фильтр)

ViewModel фильтрует коллекцию при изменении `SearchText`:

```xml
<TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
         Margin="0,0,0,8"/>
<ListBox ItemsSource="{Binding FilteredItems}"/>
```

```csharp
public string SearchText
{
    get => _search;
    set { _search = value; OnPropertyChanged(); UpdateFilter(); }
}
```

---

## TabControl (вкладки)

```xml
<TabControl>
  <TabItem Header="Общее">
    <TextBlock Text="Содержимое" Margin="8"/>
  </TabItem>
  <TabItem Header="Настройки">
    <CheckBox Content="Опция" Margin="8"/>
  </TabItem>
</TabControl>
```

---

## Menu (меню)

```xml
<Menu DockPanel.Dock="Top">
  <MenuItem Header="_Файл">
    <MenuItem Header="Открыть" Click="Open_Click"/>
    <Separator/>
    <MenuItem Header="Выход" Click="Exit_Click"/>
  </MenuItem>
</Menu>
```

---

## ToolBar (панель инструментов)

```xml
<ToolBar Tray="Top">
  <Button Content="Новый" Click="New_Click"/>
  <Separator/>
  <Button Content="Сохранить" Click="Save_Click"/>
</ToolBar>
```

---

## StatusBar (строка состояния)

```xml
<StatusBar DockPanel.Dock="Bottom">
  <StatusBarItem Content="{Binding StatusText}"/>
</StatusBar>
```

---

## ScrollViewer (прокрутка)

```xml
<ScrollViewer VerticalScrollBarVisibility="Auto">
  <StackPanel>
    <!-- длинный контент -->
  </StackPanel>
</ScrollViewer>
```

---

## Диалоги

```csharp
MessageBox.Show("Готово", "Информация", MessageBoxButton.OK, MessageBoxImage.Information);
var ok = MessageBox.Show("Удалить?", "Подтверждение", MessageBoxButton.YesNo) == MessageBoxResult.Yes;

var dlg = new OpenFileDialog { Filter = "Текст|*.txt|Все|*.*" };
if (dlg.ShowDialog() == true) { var path = dlg.FileName; }

var save = new SaveFileDialog { DefaultExt = ".txt" };
if (save.ShowDialog() == true) { /* записать */ }
```

(`Microsoft.Win32` — `OpenFileDialog`, `SaveFileDialog`.)

---

## ToolTip (подсказка)

```xml
<Button Content="?" ToolTip="Сохранить документ на диск"/>
```

---

## ContextMenu (контекстное меню)

```xml
<ListBox>
  <ListBox.ContextMenu>
    <ContextMenu>
      <MenuItem Header="Копировать" Click="Copy_Click"/>
      <MenuItem Header="Вставить" Click="Paste_Click"/>
    </ContextMenu>
  </ListBox.ContextMenu>
</ListBox>
```

---

## Дочернее окно (модальное)

```csharp
var dialog = new SettingsWindow { Owner = this };
if (dialog.ShowDialog() == true) { /* применить */ }
```

**SettingsWindow.xaml** — отдельный `Window` с `ResizeMode="NoResize"`.

---

## Общие ресурсы (Style)

**App.xaml:**

```xml
<Application.Resources>
  <Style TargetType="TextBox">
    <Setter Property="Margin" Value="0,0,0,8"/>
    <Setter Property="Padding" Value="4,2"/>
  </Style>
  <Style x:Key="PrimaryButton" TargetType="Button">
    <Setter Property="Background" Value="#2563eb"/>
    <Setter Property="Foreground" Value="White"/>
    <Setter Property="Padding" Value="12,6"/>
  </Style>
</Application.Resources>
```

```xml
<Button Content="OK" Style="{StaticResource PrimaryButton}"/>
```

---

## Привязка данных (кратко)

| XAML | Назначение |
|------|------------|
| `{Binding Path}` | свойство DataContext |
| `{Binding Name, Mode=TwoWay}` | двусторонняя |
| `{Binding Count, StringFormat=N0}` | формат |
| `{RelativeSource Self, Path=ActualWidth}` | к самому элементу |

`DataContext` задают на `Window` или корневом `Grid`: `DataContext="{Binding MainViewModel}"`.

---

## Фоновая работа

```csharp
await Task.Run(() => LongWork());
StatusText = "Готово";

// или явно
Dispatcher.Invoke(() => ProgressBar.Value = 50);
```

---

## Частые ошибки

| Симптом | Причина |
|---------|---------|
| Binding не работает | Нет `DataContext`, свойство без `INotifyPropertyChanged` |
| Окно пустое | Элемент вне видимой области, `Height=0` |
| Cross-thread | UI не через `Dispatcher` |
| XAML не собирается | Неверный `xmlns`, опечатка в теге |

---

## См. также

- [Первая форма WPF — XAML, стили и шаблоны](./119.md)
- [WinForms — элементы UI](./1152.md)
- [Справочник XAML](/encyclopedia/3-data-markup/3-04-konfiguratsii-i-dannye/6)
- [Архитектура десктопа](/encyclopedia/4-code-dev/4-11-desktopnye-prilozheniya/1) · [112.md](/encyclopedia/4-code-dev/4-11-desktopnye-prilozheniya/112.md)
- [Платформа .NET — UI-фреймворки](/encyclopedia/5-languages/5-04-platforma-dotnet/13.md)

---
